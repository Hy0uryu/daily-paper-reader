---
title: "M2H-MX: Multi-Task Dense Visual Perception for Real-Time Monocular Spatial Understanding"
title_zh: M2H-MX：用于实时单目空间理解的多任务稠密视觉感知
authors: "U. V. B. L. Udugama, George Vosselman, Francesco Nex"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29236v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 实时单目空间理解与建图
tldr: M2H-MX是一个专为机器人实时单目空间感知设计的深度学习模型。它通过轻量级解码器整合了多尺度特征、全局上下文和跨任务交互，同时预测深度和语义信息。该模型能无缝集成到单目SLAM系统中，显著提升了建图精度和轨迹稳定性。在NYUDv2和ScanNet数据集上的实验证明，它在保持实时性的同时，大幅超越了现有基准，为机器人提供了可靠的单目空间理解能力。
selection_source: fresh_fetch
motivation: 旨在解决单目相机在机器人实时空间感知中难以兼顾高精度深度估计与语义理解，且难以稳定集成到SLAM系统中的问题。
method: 提出一种轻量级多任务模型，利用寄存器门控全局上下文和受控跨任务交互机制，在严格延迟限制下实现深度与语义的互补预测。
result: "在NYUDv2上达到SOTA水平，并在ScanNet实测中将单目SLAM的轨迹误差降低了60.7%，同时生成了更清晰的度量语义地图。"
conclusion: 证明了现代多任务稠密预测模型可以可靠地部署于实时机器人系统，显著增强单目视觉的空间感知与建图性能。
---

## 摘要
单目相机因其低成本和易于部署而在机器人感知领域极具吸引力，但从单一图像流中实现可靠的实时空间理解仍然具有挑战性。尽管最近的多任务稠密预测模型提高了逐像素深度和语义估计的性能，但将这些进展转化为稳定的单目建图系统仍然并非易事。本文提出了 M2H-MX，这是一种用于单目空间理解的实时多任务感知模型。该模型在保留多尺度特征表示的同时，在轻量级解码器中引入了寄存器门控全局上下文和受控的跨任务交互，使得深度和语义预测在严格的延迟限制下能够相互增强。其输出通过一个紧凑的感知到建图接口，直接集成到未经修改的单目 SLAM 流程中。我们评估了稠密预测的准确性和在环系统性能。在 NYUDv2 数据集上，M2H-MX-L 达到了最先进的结果，与具有代表性的多任务基准模型相比，语义 mIoU 提高了 6.6%，深度 RMSE 降低了 9.4%。当部署在 ScanNet 上的实时单目建图系统中时，与强大的单目 SLAM 基准相比，M2H-MX 将平均轨迹误差降低了 60.7%，同时生成了更清晰的度量-语义地图。这些结果表明，现代多任务稠密预测可以可靠地部署在机器人系统中，用于实时单目空间感知。

## Abstract
Monocular cameras are attractive for robotic perception due to their low cost and ease of deployment, yet achieving reliable real-time spatial understanding from a single image stream remains challenging. While recent multi-task dense prediction models have improved per-pixel depth and semantic estimation, translating these advances into stable monocular mapping systems is still non-trivial.   This paper presents M2H-MX, a real-time multi-task perception model for monocular spatial understanding. The model preserves multi-scale feature representations while introducing register-gated global context and controlled cross-task interaction in a lightweight decoder, enabling depth and semantic predictions to reinforce each other under strict latency constraints. Its outputs integrate directly into an unmodified monocular SLAM pipeline through a compact perception-to-mapping interface.   We evaluate both dense prediction accuracy and in-the-loop system performance. On NYUDv2, M2H-MX-L achieves state-of-the-art results, improving semantic mIoU by 6.6% and reducing depth RMSE by 9.4% over representative multi-task baselines. When deployed in a real-time monocular mapping system on ScanNet, M2H-MX reduces average trajectory error by 60.7% compared to a strong monocular SLAM baseline while producing cleaner metric-semantic maps. These results demonstrate that modern multi-task dense prediction can be reliably deployed for real-time monocular spatial perception in robotic systems.

---

## 论文详细总结（自动生成）

### 论文总结：M2H-MX —— 用于实时单目空间理解的多任务稠密视觉感知

#### 1. 核心问题与整体含义（研究动机和背景）
单目相机在机器人感知中具有低成本、轻量化的优势，但从单帧图像流中提取可靠的几何与语义信息（空间理解）极具挑战。现有的多任务稠密预测模型虽然在精度上有所提升，但在实际部署到单目 SLAM（即时定位与建图）系统时面临三大痛点：
*   **延迟与稳定性：** 预测必须具有低且可预测的延迟，且帧间预测需保持稳定，否则会破坏追踪和地图融合。
*   **集成难度：** 多数感知模型难以在不修改 SLAM 后端的情况下直接嵌入。
*   **资源受限：** 机器人系统通常需要在有限的算力下同时运行感知、定位和规划。
**M2H-MX** 的研究动机在于弥合现代深度学习感知模型与实际单目建图系统之间的鸿沟，提供一个高性能、实时且易集成的感知前端。

#### 2. 方法论：核心思想与关键技术
M2H-MX 采用了一种基于基础模型（Foundation Model）的轻量化多任务架构，核心组件包括：
*   **骨干网络与适配（Backbone & LoRA）：** 使用冻结的 **DINOv3** 作为骨干网络，仅在最后 12 层 Transformer 块中应用 **LoRA（低秩自适应）** 进行任务微调，既保留了基础模型的泛化能力，又极大地减少了可训练参数。
*   **寄存器门控曼巴解码器（Register-Gated Mamba, RGM）：** 
    *   利用 DINOv3 的“寄存器标记（Register Tokens）”提取全局场景上下文。
    *   引入 **Mamba 块** 进行高效的长程特征传播，并通过寄存器驱动的通道门控机制将全局信息注入每个尺度的解码器中。
*   **跨任务混合器（Cross-Task Mixer, CTM）：** 允许不同任务（如深度、语义、法线、边缘）之间进行受控的特征交换。通过门控聚合机制，辅助任务可以调制目标任务的特征，增强预测的一致性。
*   **多尺度卷积注意力（MSCA）：** 在特征融合后进行残差细化，利用深度卷积空间注意力提升局部细节的准确度。
*   **感知-建图接口：** 输出直接对接 **Mono-Hydra** 等 SLAM 框架，将预测的深度与 RGB 结合生成伪 RGB-D 帧，用于里程计追踪和 3D 场景图构建。

#### 3. 实验设计
*   **数据集：** 
    *   **NYUDv2（室内）：** 评估深度、语义、法线和边缘预测。
    *   **Cityscapes（室外）：** 评估语义和视差（Disparity）预测。
    *   **ScanNet：** 用于“在环（In-the-loop）”系统评估，测试单目 SLAM 的轨迹精度和建图质量。
*   **Benchmark 与对比方法：**
    *   **感知基准：** 对比了 TaskPrompter、MTMamba、InvPT、MLoRE 等 SOTA 多任务模型。
    *   **系统基准：** 对比了 DROID-SLAM、Go-SLAM、iMAP、NICE-SLAM 等主流单目/RGB-D SLAM 方案。

#### 4. 资源与算力
*   **实时性：** 系统在 GPU 上运行感知模块，CPU 运行状态估计，实现了 **15–20 Hz** 的稳定端到端帧率。
*   **模型规模：** 提供了 M2H-MX-B（基础版）和 M2H-MX-L（大模型版）。L 版参数量约为 332M-353M，计算量约为 371-491 GFLOPs。
*   **未明确说明的信息：** 论文未详细列出具体的训练 GPU 型号（如 A100 或 H100）及总训练时长，仅强调了推理阶段的实时表现。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从单帧预测精度到下游 SLAM 系统表现的全方位测试。
*   **消融实验：** 针对 CTM、MSCA、RGM 模块以及不同的骨干网络（DINOv2, ConvNeXt）进行了详细的对比实验。
*   **客观性：** 实验通过保持 SLAM 后端（Mono-Hydra）完全不变，仅更换感知前端来验证改进，这种“控制变量”的方法有力地证明了感知质量对系统性能的直接贡献。

#### 6. 主要结论与发现
*   **精度提升：** 在 NYUDv2 上，M2H-MX-L 将语义 mIoU 提升了 6.6%，深度 RMSE 降低了 9.4%，刷新了多任务学习的记录。
*   **系统增益：** 在 ScanNet 部署中，相比于强大的单目 Go-SLAM，集成 M2H-MX 后将**平均轨迹误差（ATE）降低了 60.7%**（从 17.59cm 降至 6.91cm）。
*   **基础模型的重要性：** 消融实验显示，DINOv3 的特征质量对解码器性能至关重要，远超传统骨干网络。

#### 7. 优点
*   **系统级视角：** 不仅仅关注单帧指标，更关注感知结果在 SLAM 闭环中的稳定性和实用性。
*   **架构优雅：** 巧妙利用了 DINOv3 的寄存器标记和 Mamba 的线性复杂度特性，实现了全局上下文建模与实时性的平衡。
*   **即插即用：** 紧凑的接口设计使其能够轻松集成到现有的单目机器人流程中。

#### 8. 不足与局限
*   **骨干网络依赖：** 性能高度依赖于高性能的基础模型（DINOv3），这可能导致在算力极度受限的嵌入式设备（如低端单片机或早期 Jetson 模块）上难以运行。
*   **训练细节缺失：** 缺乏训练成本（时长、能耗）的详细报告，对于想要复现或重新训练模型的开发者参考价值有限。
*   **长程语义一致性：** 虽然轨迹误差降低，但论文未深入探讨在超大规模场景或长时间运行下，语义标签的长期一致性维护问题。

（完）
