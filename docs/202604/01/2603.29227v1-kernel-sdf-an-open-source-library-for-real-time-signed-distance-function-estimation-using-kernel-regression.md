---
title: "Kernel-SDF: An Open-Source Library for Real-Time Signed Distance Function Estimation using Kernel Regression"
title_zh: Kernel-SDF：一个使用核回归进行实时有符号距离函数估计的开源库
authors: "Zhirui Dai, Tianxing Fan, Mani Amani, Jaemin Seo, Ki Myung Brian Lee, Hyondong Oh, Nikolay Atanasov"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29227v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 使用有符号距离函数进行实时环境表示与建图
tldr: Kernel-SDF是一个开源库，旨在解决机器人应用中环境表示的实时性与不确定性量化问题。针对现有体素、神经网络及高斯过程方法的局限性，该研究提出了一种结合核回归与高斯过程回归的新框架。通过前端学习连续占用场和后端估计精确SDF，实现了高精度的SDF、梯度及不确定性估计，并支持实时网格重建，为机器人导航和规划提供了可靠的几何信息。
selection_source: fresh_fetch
motivation: 现有的SDF重建方法在处理大规模流式传感器数据时，难以平衡实时性、高分辨率、不确定性量化及计算效率。
method: 采用双端架构，前端利用核回归学习连续占用场，后端基于前端提取的表面边界样本通过高斯过程回归估计精确的SDF。
result: 实验表明Kernel-SDF在保持实时性能的同时，在SDF估计精度和不确定性校准方面均优于现有主流方法。
conclusion: Kernel-SDF为机器人提供了一个高效、准确且具备不确定性感知能力的开源环境表示工具，适用于复杂的实时应用场景。
---

## 摘要
准确且高效的环境表示对于运动规划、操作和导航等机器人应用至关重要。有符号距离函数 (SDF) 已成为一种强大的表示方法，用于编码到障碍物边界的距离，从而实现高效的碰撞检测和轨迹优化技术。然而，现有的 SDF 重建方法在处理来自流式传感器数据的大规模不确定性感知 SDF 估计时存在局限性。基于体素的方法受限于固定分辨率且缺乏不确定性量化，神经网络方法需要大量的训练时间，而高斯过程 (GP) 方法在可扩展性、符号估计和不确定性校准方面面临挑战。在本论文中，我们开发了一个开源库 Kernel-SDF，它利用核回归实时学习具有校准不确定性量化的 SDF。我们的方法由一个通过核回归学习连续占据场的前端，以及一个利用前端表面边界样本通过 GP 回归估计准确 SDF 的后端组成。Kernel-SDF 实时提供准确的 SDF、SDF 梯度、SDF 不确定性以及网格构建。评估结果表明，与现有方法相比，Kernel-SDF 在保持实时性能的同时实现了更高的精度，使其适用于各种需要可靠的不确定性感知几何信息的机器人应用。

## Abstract
Accurate and efficient environment representation is crucial for robotic applications such as motion planning, manipulation, and navigation. Signed distance functions (SDFs) have emerged as a powerful representation for encoding distance to obstacle boundaries, enabling efficient collision-checking and trajectory optimization techniques. However, existing SDF reconstruction methods have limitations when it comes to large-scale uncertainty-aware SDF estimation from streaming sensor data. Voxel-based approaches are limited by fixed resolution and lack uncertainty quantification, neural network methods require significant training time, while Gaussian process (GP) methods struggle with scalability, sign estimation, and uncertainty calibration. In this letter, we develop an open-source library, Kernel-SDF, which uses kernel regression to learn SDF with calibrated uncertainty quantification in real-time. Our approach consists of a front-end that learns a continuous occupancy field via kernel regression, and a back-end that estimates accurate SDF via GP regression using samples from the front-end surface boundaries. Kernel-SDF provides accurate SDF, SDF gradient, SDF uncertainty, and mesh construction in real-time. Evaluation results show that Kernel-SDF achieves superior accuracy compared to existing methods, while maintaining real-time performance, making it suitable for various robotics applications requiring reliable uncertainty-aware geometric information.

---

## 论文详细总结（自动生成）

以下是对论文《Kernel-SDF: An Open-Source Library for Real-Time Signed Distance Function Estimation using Kernel Regression》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在机器人应用中实现**实时、大规模、具备不确定性感知能力**的有符号距离函数（SDF）环境表示。
*   **研究动机**：
    *   **体素方法**（如 Voxblox）：受限于固定分辨率，且缺乏不确定性量化和微分特性。
    *   **神经网络方法**（如 DeepSDF）：虽然连续且可微，但在线训练收敛慢，难以满足实时流式数据处理需求。
    *   **高斯过程（GP）方法**：在处理大规模场景时计算量爆炸，且在符号估计（正负号）和不确定性校准方面表现不佳。
*   **整体含义**：提出一个名为 **Kernel-SDF** 的开源库，通过结合核回归（BHM）与高斯过程（GP），在保证实时性的同时，提供高精度的 SDF 估计及其梯度和不确定性。

### 2. 方法论
Kernel-SDF 采用双端架构，并利用八叉树（Octree）进行空间划分：
*   **前端：表面估计（BHM）**
    *   使用**贝叶斯希尔伯特映射（BHM）**学习连续的占据概率场。
    *   通过线性回归在希尔伯特空间估计权重，并利用期望最大化（EM）算法实时更新。
    *   **符号预测**：基于 BHM 的对数几率（log-odds）判断查询点在物体内部还是外部。
    *   **表面提取**：利用 Marching Cubes 算法从 BHM 中提取零水平集的表面点，并根据梯度计算表面点的不确定性。
*   **后端：SDF 预测（GP）**
    *   **Log-GP 回归**：在每个八叉树节点内，利用前端提供的表面样本训练 GP，学习无符号距离函数（UDF）。
    *   **核函数优化**：扩展了传统的 Matérn 核，引入 RBF 核并结合非线性变换和自适应缩放，解决了数值下溢问题。
    *   **不确定性量化**：提出一种基于 **Softmin** 的融合策略，将表面样本的不确定性直接传播到 SDF 预测中，确保远离表面的区域不确定性不会异常爆炸。
*   **系统优化**：
    *   **权重同步**：在相邻局部地图间同步 BHM 权重，保证全局一致性。
    *   **优先级队列更新**：根据查询频率和数据更新时间，动态调度 BHM 更新、表面提取和 GP 训练任务。

### 3. 实验设计
*   **数据集**：
    1.  **Replica**：高质量室内合成数据集（带模拟传感器噪声）。
    2.  **Cow and Lady**：经典的真实世界 RGB-D 数据集。
    3.  **Newer College**：大规模室外 LiDAR 数据集。
*   **对比方法（Benchmarks）**：
    *   **Voxblox**（基于体素的 TSDF/ESDF）。
    *   **FIESTA**（快速增量体素 ESDF）。
    *   **iSDF**（实时神经 SDF）。
    *   **VDB-GPDF**（基于 VDB 结构的 GP 距离场）。
*   **评估指标**：网格重建质量（F1 分数、精度、召回率、Chamfer-L1 距离）、SDF 估计误差（MAE）、梯度误差、计算耗时（每帧处理时间、查询延迟）。

### 4. 资源与算力
*   **硬件平台**：Intel i9-14900K CPU，NVIDIA RTX 3090 GPU。
*   **实时性表现**：
    *   平均每帧处理时间（FPT）约为 **110ms - 190ms**。
    *   1000 个查询点的预测延迟仅为 **2.8ms - 4.4ms**。
*   **软件接口**：提供 C++ 实现，并支持 Python、ROS1 和 ROS2 接口。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个 Replica 场景、2 个真实世界场景（Cow/Lady, Newer College）中进行了详尽测试。
*   **消融实验**：专门针对**传感器噪声**的影响进行了消融研究，验证了系统在不同噪声水平下的鲁棒性。
*   **应用案例**：展示了在真实 Jackal 机器人上利用 Kernel-SDF 进行“安全气泡（Safe Bubble）”路径规划的实际案例。
*   **评价**：实验设计覆盖了从合成到真实、从室内到室外、从视觉到 LiDAR 的多种场景，对比方法涵盖了当前主流的各类技术路线，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **精度领先**：Kernel-SDF 在网格重建和 SDF 距离估计精度上均优于所有基准方法，尤其是在处理噪声数据时表现更稳健。
*   **不确定性校准**：提出的 Softmin 融合方法生成的方差图与实际 SDF 误差高度相关，能够为风险感知决策提供可靠依据。
*   **实时性与扩展性**：通过八叉树分区和优先级调度，系统能够在大规模场景下保持稳定的实时处理能力。
*   **梯度质量**：相比 VDB-GPDF 等方法，Kernel-SDF 提供的 SDF 梯度更加准确，有利于基于梯度的运动规划。

### 7. 优点
*   **双端解耦设计**：将复杂的 SDF 学习分解为“占据场前端”和“距离场后端”，兼顾了符号估计的鲁棒性和距离估计的准确性。
*   **不确定性感知**：解决了传统 GP 方法在远离表面时方差估计失效的问题，提供了物理意义明确的不确定性量化。
*   **高度优化**：通过权重同步、稀疏核计算和优先级队列，显著提升了非参数化方法的计算效率。
*   **开源贡献**：提供了完整的机器人中间件支持（ROS1/2），易于集成到现有机器人系统中。

### 8. 不足与局限
*   **超参数敏感性**：核函数缩放因子 $\lambda$、八叉树分辨率等参数可能需要针对不同传感器（如 LiDAR vs 深度相机）进行微调。
*   **内存开销**：虽然使用了八叉树，但作为非参数化方法，存储大量局部 GP 样本和 BHM 权重在极端大规模场景下仍可能面临内存压力。
*   **动态环境限制**：虽然论文提到能适应动态环境，但主要实验仍集中在静态场景重建，对于高速运动物体的实时追踪和擦除效果有待进一步验证。

（完）
