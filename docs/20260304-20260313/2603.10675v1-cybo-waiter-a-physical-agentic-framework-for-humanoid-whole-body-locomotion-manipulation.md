---
title: "Cybo-Waiter: A Physical Agentic Framework for Humanoid Whole-Body Locomotion-Manipulation"
title_zh: Cybo-Waiter：一种用于类人机器人全身移动-操作的物理智能体框架
authors: "Peng Ren, Haoyang Ge, Chuan Qi, Cong Huang, Hong Li, Jiang Zhao, Pei Chi, Kai Chen"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10675v1"
tags: ["query:hr-rob"]
score: 10.0
evidence: 结合VLM规划与3D几何监督的人形机器人全身移动与操作框架
tldr: 针对人形机器人在复杂环境中执行长程自然语言任务的挑战，本文提出了 Cybo-Waiter 框架。该框架利用大视觉语言模型（VLM）将指令转化为可验证的任务程序，并通过 SAM3 和 RGB-D 实现多目标 3D 几何监督。通过协调全身运动与操作，并结合基于谓词的反馈机制进行重规划，显著提升了机器人在处理复杂任务时的鲁棒性和成功率。
selection_source: fresh_fetch
motivation: 旨在解决人形机器人在执行长程任务时，移动与操作紧密耦合以及环境部分可观测性导致的执行不可靠问题。
method: 开发了一个物理智能体框架，将 VLM 规划转化为带谓词验证的 JSON 任务序列，并结合 3D 几何感知与全身运动原语协调。
result: 在桌面操作和长程移动操作实验中，该框架通过多目标定位和恢复驱动的重规划显著增强了任务执行的鲁棒性。
conclusion: 研究证明了将高级语义规划、底层几何验证与全身协调控制相结合，能有效提升人形机器人在人类环境中的作业能力。
---

## 摘要
机器人越来越多地被要求在人类环境中执行开放式自然语言请求，这要求在部分可观测性下实现可靠的长程执行。这对类人机器人来说尤其具有挑战性，因为移动和操作通过姿态、可达性和平衡紧密耦合。我们提出了一种类人智能体框架，该框架将视觉语言模型（VLM）规划转化为可验证的任务程序，并通过多物体3D几何监督实现闭环。VLM规划器将每条指令编译为带有显式基于谓词的前置条件和成功条件的类型化JSON子任务序列。利用SAM3和RGB-D，我们在3D空间中定位所有任务相关实体，估计物体质心和范围，并在稳定帧上评估谓词以获得条件级诊断。监督器利用这些诊断来验证子任务的完成情况，并为推进和重新规划提供条件级反馈。我们通过协调类人机器人的移动和全身操作来执行每个子任务，在可达性和平衡约束下选择可行的运动原语。在桌面操作和长程类人机器人移动-操作任务上的实验表明，多物体定位、时间稳定性和恢复驱动的重新规划提高了系统的鲁棒性。

## Abstract
Robots are increasingly expected to execute open ended natural language requests in human environments, which demands reliable long horizon execution under partial observability. This is especially challenging for humanoids because locomotion and manipulation are tightly coupled through stance, reachability, and balance. We present a humanoid agent framework that turns VLM plans into verifiable task programs and closes the loop with multi object 3D geometric supervision. A VLM planner compiles each instruction into a typed JSON sequence of subtasks with explicit predicate based preconditions and success conditions. Using SAM3 and RGB-D, we ground all task relevant entities in 3D, estimate object centroids and extents, and evaluate predicates over stable frames to obtain condition level diagnostics. The supervisor uses these diagnostics to verify subtask completion and to provide condition-level feedback for progression and replanning. We execute each subtask by coordinating humanoid locomotion and whole-body manipulation, selecting feasible motion primitives under reachability and balance constraints. Experiments on tabletop manipulation and long horizon humanoid loco manipulation tasks show improved robustness from multi object grounding, temporal stability, and recovery driven replanning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Cybo-Waiter: A Physical Agentic Framework for Humanoid Whole-Body Locomotion-Manipulation》的结构化深入总结：

### 1. 论文的核心问题与整体含义
论文针对人形机器人在人类真实环境中执行**长程（Long-horizon）自然语言指令**的挑战。核心难点在于：
*   **耦合性**：人形机器人的移动（Locomotion）与操作（Manipulation）在姿态、可达性和平衡方面高度耦合。
*   **感知不确定性**：环境的部分可观测性、传感器噪声和遮挡会导致执行中断。
*   **缺乏验证机制**：现有框架往往缺乏对任务进度的显式几何验证，导致失败后难以恢复。
**研究动机**是构建一个“物理智能体”框架，通过视觉语言模型（VLM）生成可验证的计划，并利用 3D 几何监督实现闭环控制。

### 2. 方法论：核心思想与关键技术
Cybo-Waiter 采用分层架构，将高级语义推理与底层物理执行解耦：
*   **结构化 VLM 任务分解**：VLM 将指令编译为类型化的 **JSON 子任务序列**。每个子任务包含显式的**前置条件（Preconditions）**和**成功条件（Success Conditions）**，以谓词（Predicate）形式表达（如 `VISIBLE`, `SUPPORTED_BY`）。
*   **多物体 3D 几何定位**：
    *   利用 **SAM3**（Segment Anything Model 3）结合 RGB-D 数据进行任务相关实体的分割。
    *   通过反投影深度像素估计物体的 3D 质心、空间范围和方向。
    *   **候选选择公式**：通过综合语义一致性、关系一致性、几何可行性和时间稳定性来选择最佳实例：$S_e(k) = w_s S_{sem} + w_r S_{rel} + w_g S_{geo} + w_t S_{temp}$。
*   **几何监督器（Supervisor）**：
    *   在连续帧（`stable_frames`）上评估谓词的满足情况，以过滤瞬时噪声。
    *   提供条件级诊断（如物体与支撑面的高度差、对齐角度），用于触发针对性的恢复动作（如重新观察、重新定位）。
*   **全身执行层**：
    *   **移动**：基于强化学习（RL）的步态库，包含 12 个动作剪辑，映射为 7 种移动原语（如侧步、转身、搬运走）。
    *   **操作**：上半身采用基于 MPC（模型预测控制）的轨迹生成，下半身独立控制以维持平衡。

### 3. 实验设计
*   **硬件平台**：Unitree G1 人形机器人，配备 Dex3-1 灵巧手和头戴式 RGB-D 相机。
*   **实验场景**：杂乱的室内办公环境，包含家具和日常用品。
*   **Benchmark 与对比方法**：
    *   **Being-0 对标实验**：在 Fetch-bottle, Deliver-basket 等 5 个任务上与 Being-0 框架进行对比。
    *   **长程任务实验**：新增了 Tidy-desk（清理桌面）、Tabletop-sorting（分类）、Bring-me-a-drink（寻找用户并递送饮料）三个更复杂的任务。
    *   **消融实验**：对比了“完整系统”与“禁用监督器（W/O Supervisor）”的版本。

### 4. 资源与算力
*   **计算架构**：采用双机设置。
*   **GPU**：桌面工作站配备 **NVIDIA RTX 5080 GPU**，负责运行感知（SAM3）、VLM 规划和监督器。
*   **机器人端**：Unitree G1 自带的板载电脑负责运行底层控制栈（RL 策略和 MPC）。
*   **训练细节**：文中提到 RL 移动策略是在模拟器中训练的，但未明确给出具体的训练时长或 GPU 集群规模。

### 5. 实验数量与充分性
*   **实验规模**：每个任务进行了 **10 次独立试验**。
*   **充分性评价**：
    *   **客观性**：通过与现有 SOTA（Being-0）在相同任务上对比，证明了其在操作成功率上的优势。
    *   **消融分析**：通过对比实验明确展示了“监督器”在长程任务中减少早熟终止、提升鲁棒性的关键作用。
    *   **局限性**：10 次试验的样本量在机器人领域属于标准配置，但对于统计显著性而言仍有提升空间；实验环境主要集中在室内办公场景，多样性尚可。

### 6. 主要结论与发现
*   **鲁棒性提升**：通过引入基于谓词的 3D 几何监督，系统能有效处理感知噪声，在长程任务中的表现显著优于无监督版本（如 Tidy-desk 成功率从 5/10 提升至 7/10）。
*   **反馈驱动的重规划**：当监督器检测到前置条件不满足时，触发的“重新观察”或“重新定位”等恢复动作是完成复杂任务的关键。
*   **全身协调**：解耦的上半身 MPC 与下半身 RL 步态库能够有效平衡人形机器人的稳定性和操作灵活性。

### 7. 优点（亮点）
*   **闭环验证**：将 VLM 的高级推理与基于 SAM3 的底层几何特征紧密结合，解决了“幻觉”或执行盲目性的问题。
*   **结构化接口**：定义的 JSON 任务模式非常清晰，便于扩展新的谓词和动作原语。
*   **时间稳定性**：引入 `stable_frames` 机制，极大增强了系统在动态或噪声环境下的判断可靠性。

### 8. 不足与局限
*   **语义验证依赖**：在几何信息缺失或模糊时，系统仍需调用 VLM 进行语义验证，这可能引入推理延迟或不确定性。
*   **恢复策略有限**：目前的恢复动作（Recovery Operators）集合较小，面对极度复杂的物理失败（如物体滑落后的复杂姿态）可能仍需更强的重规划能力。
*   **实时性挑战**：虽然使用了 RTX 5080，但 SAM3 和 VLM 的串联运行在处理极高动态任务时可能存在延迟瓶颈。

（完）
