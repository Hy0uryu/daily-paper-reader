---
title: "GSMem: 3D Gaussian Splatting as Persistent Spatial Memory for Zero-Shot Embodied Exploration and Reasoning"
title_zh: GSMem：基于 3D 高斯泼溅的持久化空间记忆，用于零样本具身探索与推理
authors: "Yiren Lu, Yi Du, Disheng Liu, Yunlai Zhou, Chen Wang, Yu Yin"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19137v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 用于持久空间记忆和探索的3D高斯泼溅技术
tldr: GSMem 提出将 3D 高斯泼溅（3DGS）作为具身智能体的持久空间记忆，解决了传统场景表示中观测缺失不可恢复的问题。通过 3DGS 的连续几何和外观参数化，智能体能够实现“空间回想”，从最优视角渲染新视图以供 VLM 推理。结合并行场景图与语义语言场，GSMem 实现了鲁棒的目标定位与混合探索策略，在零样本具身问答和导航任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的场景表示缺乏事后重新观测的能力，一旦初始观测遗漏目标，导致的记忆缺失往往无法弥补。
method: 利用 3DGS 构建持久空间记忆以实现空间回想，并结合并行场景图、语义语言场及 VLM 驱动的混合探索策略进行目标定位与推理。
result: 在具身问答和终身导航任务中的广泛实验证明了该框架在定位目标区域和生成高保真推理视图方面的鲁棒性与有效性。
conclusion: GSMem 通过引入 3DGS 作为连续且可重观测的空间记忆，显著增强了智能体在零样本环境下的具身探索与推理能力。
---

## 摘要
有效的具身探索要求智能体能够随时间积累并保留空间知识。然而，现有的场景表示（如离散场景图或基于静态视图的快照）缺乏“事后可重观测性”（post-hoc re-observability）。如果初始观测遗漏了目标，由此产生的记忆缺失通常是不可恢复的。为了弥补这一差距，我们提出了 GSMem，这是一个基于 3D 高斯泼溅（3DGS）的零样本具身探索与推理框架。通过显式地参数化连续几何和密集外观，3DGS 作为一种持久化空间记忆，赋予了智能体“空间回想”（Spatial Recollection）的能力：即从先前未占据的最佳视角渲染出逼真的新视图。为了实现这一点，GSMem 采用了一种检索机制，同时利用并行的对象级场景图和语义级语言场。这种互补设计能够稳健地定位目标区域，使智能体能够为高保真视觉语言模型（VLM）推理“幻化”出最佳视图。此外，我们引入了一种混合探索策略，将 VLM 驱动的语义评分与基于 3DGS 的覆盖目标相结合，平衡了任务感知探索与几何覆盖。在具身问答和终身导航方面的广泛实验证明了我们框架的稳健性和有效性。

## Abstract
Effective embodied exploration requires agents to accumulate and retain spatial knowledge over time. However, existing scene representations, such as discrete scene graphs or static view-based snapshots, lack \textit{post-hoc re-observability}. If an initial observation misses a target, the resulting memory omission is often irrecoverable. To bridge this gap, we propose \textbf{GSMem}, a zero-shot embodied exploration and reasoning framework built upon 3D Gaussian Splatting (3DGS). By explicitly parameterizing continuous geometry and dense appearance, 3DGS serves as a persistent spatial memory that endows the agent with \textit{Spatial Recollection}: the ability to render photorealistic novel views from optimal, previously unoccupied viewpoints. To operationalize this, GSMem employs a retrieval mechanism that simultaneously leverages parallel object-level scene graphs and semantic-level language fields. This complementary design robustly localizes target regions, enabling the agent to ``hallucinate'' optimal views for high-fidelity Vision-Language Model (VLM) reasoning. Furthermore, we introduce a hybrid exploration strategy that combines VLM-driven semantic scoring with a 3DGS-based coverage objective, balancing task-aware exploration with geometric coverage. Extensive experiments on embodied question answering and lifelong navigation demonstrate the robustness and effectiveness of our framework

---

## 论文详细总结（自动生成）

以下是对论文《GSMem: 3D Gaussian Splatting as Persistent Spatial Memory for Zero-Shot Embodied Exploration and Reasoning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的具身智能体场景表示（如离散场景图或静态图像快照）缺乏**“事后可重观测性” (Post-hoc Re-observability)**。
*   **研究动机**：
    *   **记忆不可恢复性**：如果智能体在初始探索时因视角不佳或遮挡遗漏了目标，离散标签或静态快照无法提供补救机会。
    *   **视角依赖性**：现有的视图表示是稀疏且视角依赖的，导致视觉语言模型（VLM）在推理时缺乏足够的几何细节和上下文。
    *   **目标**：利用 3D 高斯泼溅（3DGS）构建一种连续、密集且可随时重新渲染的持久空间记忆，使智能体具备“空间回想”能力。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将 3DGS 作为环境的底层表示，结合语义语言场和对象场景图，通过“幻化”最优视角来支持 VLM 的推理和决策。
*   **关键技术细节**：
    1.  **3DGS 映射与在线语言场**：
        *   使用滑动窗口优化 3DGS 参数。
        *   **无优化语言场生成**：通过“权重一致的反向聚合”技术，将 2D CLIP 特征直接提升到 3D 高斯点上，无需额外优化开销。
    2.  **多级检索-渲染机制**：
        *   **对象级检索**：利用 3D 场景图定位候选对象。
        *   **语义级检索**：在 3D 语言场中通过 CLIP 嵌入相似度定位感兴趣区域（ROI）。
    3.  **最优视角选择（Sample-then-score）**：
        *   在 ROI 周围采样候选位姿，通过**可见性评分**（射线检测）、**投影面积评分**（尺度控制）和**不透明度评分**（渲染质量）筛选出最佳渲染视角。
    4.  **混合探索策略**：
        *   **语义相关性**：VLM 对前沿点（Frontiers）进行评分。
        *   **几何覆盖（信息增益）**：利用 3DGS 的 Fisher 信息矩阵（FIM）的迹（T-optimality）来量化区域的不确定性，引导智能体探索未观测区域。

### 3. 实验设计
*   **数据集与场景**：
    *   **Active EQA**：使用 OpenEQA 基准，涵盖 Habitat-Matterport3D (HM3D) 中的 63 个场景，共 184 个问题。
    *   **Lifelong Navigation**：使用 GOAT-Bench 的“Val Unseen”划分，包含 36 个场景，超过 2600 个子任务。
*   **对比方法**：
    *   盲 LLM（GPT-4o）、带描述的 LLM（LLaVA）。
    *   具身探索基准：Explore-EQA、ConceptGraphs、3D-Mem、TANGO、MTU3D 等。
*   **Benchmark 指标**：LLM-Match、LLM-Match SPL（效率加权）、成功率（SR）和 SPL。

### 4. 资源与算力
*   **硬件配置**：
    *   导航框架运行在单张 **NVIDIA RTX 4090** GPU 上。
    *   VLM 推理（如 Qwen3-VL-8B）部署在 **NVIDIA H100** GPU 上。
*   **运行效率**：在多进程设置下，每步平均处理时间约为 **1.2 秒**（包含地图更新、检索渲染和 VLM 推理）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流具身智能基准上进行了大规模测试，任务涵盖对象识别、空间推理和长程导航。
    *   **消融实验**：针对 CLIP 语言场、视角选择、扩散模型增强和混合探索策略分别进行了消融分析，使用了 GOAT-Bench 10% 的数据量。
*   **充分性与客观性**：实验对比了从强化学习到最新 VLM 驱动的多种 SOTA 方法，并统一使用 GPT-4o 作为 VLM 后端以确保公平性。实验设计涵盖了从感知失败到推理失败的多种案例分析，较为全面。

### 6. 主要结论与发现
*   **性能领先**：GSMem 在 Active EQA 和终身导航任务中均取得了 SOTA 性能，尤其在长程任务中优势明显。
*   **空间回想的价值**：通过 3DGS 渲染的最优视角显著提升了 VLM 回答问题的准确性，解决了初始观测视角差的问题。
*   **鲁棒性**：即使对象检测器（场景图）漏检，语义语言场仍能通过相似度检索定位目标，增强了系统的容错能力。

### 7. 优点与亮点
*   **创新的记忆表示**：首次将 3DGS 作为具身智能体的持久空间记忆，实现了“事后可重观测性”。
*   **高效的特征提升**：提出的无优化语言场更新方法极大地降低了在线建图的计算负担。
*   **混合探索机制**：结合了 VLM 的高层语义引导和基于信息论（Fisher 信息）的底层几何覆盖，使探索既有目的性又全面。

### 8. 不足与局限
*   **实时性挑战**：虽然 1.2s/步在研究中尚可，但在高速移动或高频交互的实时机器人应用中仍有延迟压力。
*   **环境假设**：3DGS 通常假设环境是静态的，论文未深入讨论在动态环境（如有人走动）下的表现。
*   **依赖性**：系统性能高度依赖于底层 VLM 的推理能力和 CLIP 特征的判别力，在极端长尾分布的对象上可能仍存在检索困难。

（完）
