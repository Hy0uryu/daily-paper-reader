---
title: Proprioceptive-only State Estimation for Legged Robots with Set-Coverage Measurements of Learned Dynamics
title_zh: 结合学习动力学集合覆盖测量的足式机器人仅本体感知状态估计
authors: "Abhijeet M. Kulkarni, Ioannis Poulakakis, Guoquan Huang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18308v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 足式机器人的纯本体感受状态估计
tldr: 本研究针对足式机器人提出了一种仅依赖本体感知的状态估计框架。针对现有基于学习的测量模型在有限数据下高斯噪声假设失效导致估计发散的问题，该方法利用集合覆盖（Set-Coverage）表述来刻画测量噪声，无需预设分布。通过将集合覆盖测量系统地集成到高斯滤波器中，实现了计算高效且鲁棒的估计。在仿真和真实四足机器人数据集上的实验表明，该方法在真实噪声环境下比传统高斯基准更具一致性且不易漂移。
selection_source: fresh_fetch
motivation: 现有的基于学习的本体感知状态估计方法通常假设高斯噪声，但在训练数据有限时，这种假设会导致估计不一致甚至发散。
method: 提出一种不依赖分布假设的集合覆盖测量方法来刻画学习动力学的噪声，并将其与高斯滤波器系统性结合。
result: 在仿真和真实四足机器人实验中，该方法在处理真实噪声时表现出更强的一致性，且漂移显著降低。
conclusion: 集合覆盖测量为足式机器人在感知受限环境下的状态估计提供了一种比传统高斯模型更稳健、更实用的方案。
---

## 摘要
仅本体感知状态估计对于足式机器人具有吸引力，因为其计算开销更低，且不受感知退化环境的影响。关节级测量的历史记录包含丰富的信息，可用于推断系统动力学并随后产生导航测量。最近的方法利用学习到的测量模型产生这些估计，并在高斯噪声假设下将其与 IMU 数据融合。然而，在训练数据有限的情况下，这一假设很容易失效，导致估计不一致并可能发散。在这项工作中，我们为足式机器人提出了一种仅本体感知的状态估计框架，该框架使用不假设任何分布的集合覆盖陈述来表征测量噪声。我们开发了一种实用且计算开销低的方法，以系统化的方式将这些集合覆盖测量与高斯滤波器结合使用。我们在仿真和两个真实的四足机器人数据集上验证了该方法。与高斯基准方法的对比表明，我们提出的方法在真实噪声场景下保持了一致性，且不易发生漂移。

## Abstract
Proprioceptive-only state estimation is attractive for legged robots since it is computationally cheaper and is unaffected by perceptually degraded conditions. The history of joint-level measurements contains rich information that can be used to infer the dynamics of the system and subsequently produce navigational measurements. Recent approaches produce these estimates with learned measurement models and fuse with IMU data, under a Gaussian noise assumption. However, this assumption can easily break down with limited training data and render the estimates inconsistent and potentially divergent. In this work, we propose a proprioceptive-only state estimation framework for legged robots that characterizes the measurement noise using set-coverage statements that do not assume any distribution. We develop a practical and computationally inexpensive method to use these set-coverage measurements with a Gaussian filter in a systematic way. We validate the approach in both simulation and two real-world quadrupedal datasets. Comparison with the Gaussian baselines shows that our proposed method remains consistent and is not prone to drift under real noise scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《Proprioceptive-only State Estimation for Legged Robots with Set-Coverage Measurements of Learned Dynamics》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：足式机器人在极端环境（如烟雾、黑暗、无纹理区域）下，依赖视觉或激光雷达（外感知）的导航容易失效。仅依靠本体感知（IMU和关节编码器）的状态估计更具鲁棒性。
*   **核心挑战**：现有的方法利用深度学习将关节数据历史映射为机器人速度（伪测量），并假设其噪声符合**零均值高斯分布**。但在训练数据有限或遇到未见过的工况时，这种假设会失效，导致滤波器过于自信、产生漂移甚至发散。
*   **整体含义**：本文提出一种新的框架，放弃了不切实际的高斯噪声假设，转而使用**集合覆盖（Set-Coverage）**表述来刻画学习动力学模型的误差，从而在非高斯、有偏噪声环境下实现更稳健的状态估计。

### 2. 方法论
*   **核心思想**：将学习到的速度预测器视为一个“虚拟传感器”，其误差不建模为概率密度函数，而是建模为一个**校准集合**。只要真实误差以预定概率（如85%）落在该集合内即可。
*   **关键技术细节**：
    1.  **动力学观测器**：使用轻量级的 **GRU（门控循环单元）** 网络，输入关节角度、速度和扭矩的历史序列，输出机器人体坐标系下的速度预测。
    2.  **集合覆盖表述**：利用**符合预测（Conformal Prediction）**技术，在校准数据集上确定误差边界 $\epsilon$，使得 $P(|e_t| \le \epsilon) \ge \gamma$，其中 $\gamma$ 是置信水平。
    3.  **KL散度最小化更新**：在滤波器更新阶段，寻找一个与先验分布最接近（KL散度最小）且满足集合覆盖约束的后验分布。论文证明了该优化问题存在解析解，即对先验分布进行分段重缩放。
    4.  **高斯矩匹配（Moment Matching）**：由于最优后验是非高斯的，通过矩匹配将其投影回高斯分布，以便在不变扩展卡尔曼滤波器（IEKF）框架下进行递归估计。
    5.  **降维实现**：为了计算效率，将复杂的15维状态空间积分简化为3维测量空间（速度）的积分。

### 3. 实验设计
*   **数据集与场景**：
    *   **Vision60 数据集**：作者自采，包含室内行走、急转弯、在湿滑地面行走等场景，使用动捕系统作为真值。
    *   **Spot 数据集**：公开数据集，包含长距离户外行走、上下楼梯、斜坡、隧道等复杂地形。
*   **对比方法（Benchmarks）**：
    *   **Leg IEKF**：经典的基于足端运动学和接触检测的方法。
    *   **Vel. IEKF**：基于学习速度预测但假设高斯噪声的方法。
    *   **GaRLILEO**：基于雷达-腿部-惯性融合的高级里程计（仅在Spot数据集对比）。
*   **实验设置**：包括蒙特卡洛仿真（验证有偏噪声下的鲁棒性）和真实机器人长距离轨迹回放。

### 4. 资源与算力
*   **网络结构**：单层 GRU，隐藏层大小为 64，属于极轻量级模型，旨在防止在有限数据上过拟合。
*   **计算效率**：在笔记本电脑 CPU（**Intel i9-13950HX**）上，每次测量更新仅需 **0.07 毫秒**。
*   **训练细节**：论文未详细说明 GPU 型号，但提到网络非常小，且仅在单个代表性序列上进行训练，强调了在“有限数据”下的表现。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多次蒙特卡洛仿真实验，对比了无偏高斯噪声和有偏非高斯噪声。
    *   在 Vision60 上测试了 7 个序列，在 Spot 上测试了 10 个序列，涵盖了多种步态和地形。
*   **充分性与客观性**：实验设计较为充分。通过在两个完全不同的机器人平台（Vision60 和 Spot）上验证，证明了方法的通用性。对比了包括感知融合（雷达）在内的强基准，结果显示该方法在仅用本体感知的情况下达到了接近外感知的精度。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：在真实噪声（非高斯、有偏）环境下，传统高斯 EKF 容易发散，而集合覆盖方法能保持一致性，有效抑制漂移。
*   **精度竞争力**：在 Spot 数据集的多个挑战性序列（如上下楼梯、过天桥）中，该方法显著优于传统的腿部运动学方法和高斯假设的学习方法。
*   **计算可行性**：证明了非高斯约束可以通过矩匹配高效地集成到实时滤波器中，无需昂贵的粒子滤波。

### 7. 优点
*   **理论创新**：首次将符合预测（Conformal Prediction）与不变卡尔曼滤波结合，解决了学习模型误差难以建模的问题。
*   **分布无关性**：不依赖于噪声是高斯的假设，对模型预测偏差（Epistemic Uncertainty）具有天然的抵抗力。
*   **极高效率**：亚毫秒级的更新速度，完全满足足式机器人高频控制（如 500Hz-1kHz）的需求。

### 8. 不足与局限
*   **保守性风险**：如果校准数据不能很好地代表测试环境，计算出的边界 $\epsilon$ 可能会过于保守，导致滤波器更新步长过小。
*   **矩匹配近似**：虽然矩匹配能维持高斯性，但在数学上并不能完全保证更新后的分布严格满足原始的集合覆盖概率 $\gamma$（尽管实验表明效果良好）。
*   **依赖 IMU 传播**：该方法本质上仍是里程计，长期运行仍会存在累计误差，无法完全替代具有回环检测功能的 SLAM 系统。

（完）
