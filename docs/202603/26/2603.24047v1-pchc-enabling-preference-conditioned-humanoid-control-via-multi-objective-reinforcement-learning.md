---
title: "PCHC: Enabling Preference Conditioned Humanoid Control via Multi-Objective Reinforcement Learning"
title_zh: PCHC：通过多目标强化学习实现偏好调节的人形机器人控制
authors: "Huanyu Li, Dewei Wang, Xinmiao Wang, Xinzhe Liu, Peng Liu, Chenjia Bai, Xuelong Li"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24047v1"
tags: ["query:hr-rob"]
score: 10.0
evidence: 基于多目标强化学习的人形机器人偏好调节控制
tldr: 针对人形机器人在速度与能耗等竞争目标间难以平衡的问题，本文提出了一种基于多目标强化学习的偏好条件控制框架（PCHC）。该框架通过Beta分布对齐机制和偏好向量调制的混合专家模型（MoE），使单一策略能够根据实时输入的偏好条件展现出多样化的行为。实验证明，该方法在仿真和现实世界中均能有效实现目标优先级的动态调整，为复杂多目标控制提供了高效解决方案。
selection_source: fresh_fetch
motivation: 解决人形机器人多目标控制中固定权重策略无法灵活平衡竞争目标（如速度与能耗）的问题。
method: 提出一种基于多目标强化学习的框架，利用Beta分布对齐机制和偏好向量调制的混合专家模型（MoE）实现单一策略的偏好调节。
result: 仿真和实物实验证明，该框架能让机器人在实时输入偏好条件下，灵活且多样化地调整其行为优先级。
conclusion: PCHC 成功实现了单一策略下的多目标帕累托前沿覆盖，显著提升了人形机器人在复杂任务中的适应性。
---

## 摘要
人形机器人通常需要平衡相互竞争的目标，例如在最大化速度的同时最小化能量消耗。虽然目前的强化学习（RL）方法可以掌握跌倒恢复和感知运动等复杂技能，但它们受限于固定的权重策略，只能产生单一的次优策略，而无法为复杂的多目标控制提供多样化的解决方案集。在本文中，我们提出了一个利用多目标强化学习（MORL）来实现偏好调节的人形机器人控制（PCHC）的新型框架。与需要训练一系列策略来逼近帕累托前沿（Pareto front）的传统方法不同，我们的框架使单一的偏好调节策略能够展现出广泛的多样化行为。为了有效地整合这些需求，我们引入了一种基于 Beta 分布的对齐机制，该机制通过偏好向量来调制混合专家（MoE）模块。我们在两个具有代表性的人形机器人任务上验证了我们的方法。大量的仿真和真实世界实验表明，所提出的框架允许机器人根据输入的偏好条件实时自适应地调整其目标优先级。

## Abstract
Humanoid robots often need to balance competing objectives, such as maximizing speed while minimizing energy consumption. While current reinforcement learning (RL) methods can master complex skills like fall recovery and perceptive locomotion, they are constrained by fixed weighting strategies that produce a single suboptimal policy, rather than providing a diverse set of solutions for sophisticated multi-objective control. In this paper, we propose a novel framework leveraging Multi-Objective Reinforcement Learning (MORL) to achieve Preference-Conditioned Humanoid Control (PCHC). Unlike conventional methods that require training a series of policies to approximate the Pareto front, our framework enables a single, preference-conditioned policy to exhibit a wide spectrum of diverse behaviors. To effectively integrate these requirements, we introduce a Beta distribution-based alignment mechanism based on preference vectors modulating a Mixture-of-Experts (MoE) module. We validated our approach on two representative humanoid tasks. Extensive simulations and real-world experiments demonstrate that the proposed framework allows the robot to adaptively shift its objective priorities in real-time based on the input preference condition.

---

## 论文详细总结（自动生成）

以下是对论文《PCHC: Enabling Preference Conditioned Humanoid Control via Multi-Objective Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：人形机器人在实际应用中常面临多个相互冲突的目标（如追求速度与降低能耗、步幅长度与抗干扰稳定性）。
*   **研究动机**：
    *   **现有方法的局限**：传统的强化学习（RL）通常采用预定义的固定权重将多个奖励函数标量化，导致生成的策略是单一且僵化的，无法根据环境变化或用户需求动态调整行为。
    *   **多目标挑战**：虽然多目标强化学习（MORL）旨在寻找帕累托前沿（Pareto front），但现有的多策略方法训练成本高，且在人形机器人这种高维连续控制空间中难以应用。
    *   **需求**：需要一种单一的、可根据“偏好向量”实时调节行为的统一策略框架。

### 2. 方法论
PCHC 框架的核心思想是将人形机器人控制建模为多目标马尔可夫决策过程（MOMDP），并通过偏好调节实现动态控制。
*   **偏好条件注入（PCI）模块**：
    *   **核心机制**：引入基于 **Beta 分布** 的混合专家模型（MoE）。
    *   **技术细节**：偏好向量 $\lambda$ 不直接输入网络，而是用于参数化 Beta 分布的形状参数（$\alpha, \beta$）。通过在该分布上采样路由权重来聚合多个专家网络的输出。
    *   **优势**：Beta 分布在 $[0, 1]$ 区间内的数学特性能够实现专家之间平滑且结构化的插值，从而更好地覆盖帕累托前沿。
*   **多评论家（Multi-Critic）架构**：
    *   为每个特定目标（如能耗、步幅）设置独立的 Critic，并设置一个额外的任务 Critic（负责基础稳定性）。
    *   **优势函数计算**：最终的优势函数 $\hat{A}_\lambda$ 是各目标优势函数的加权和加上任务优势函数，确保在满足偏好的同时维持基本任务目标。
*   **对抗运动先验（AMP）**：集成 AMP 奖励以确保机器人动作的自然性，避免强化学习中常见的“非人”姿态。

### 3. 实验设计
*   **实验场景**：
    1.  **行走任务（Walking）**：平衡“步幅长度”与“抗干扰能力”。
    2.  **跌倒恢复任务（Fall Recovery）**：平衡“能耗效率”与“抗干扰能力”。
*   **对比基准（Benchmarks）**：
    *   **HoST**：一种先进的人形机器人起身控制方法（作为跌倒恢复的基准）。
    *   **固定权重 RL**：传统的单目标强化学习方法。
    *   **消融实验**：对比了有无 PCI 模块（使用标准门控网络）的效果。
    *   **固定偏好策略**：对比单一策略（PCHC）与多个专门为固定偏好训练的策略的性能差异。
*   **评估指标**：超体积（Hypervolume，衡量帕累托前沿覆盖度）、稀疏度（Sparsity，衡量解的均匀性）、成功率、能耗、步幅等。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA GeForce RTX 4090 GPU** 进行训练。
*   **仿真平台**：基于 Isaac Gym 高性能并行仿真器。
*   **训练规模**：
    *   每个任务训练 **12,000 次迭代**。
    *   每次迭代聚合来自 **4,096 个并行环境** 的经验。
    *   控制频率为 50Hz，底层 PD 控制器频率为 500Hz。
*   **实物部署**：策略被转换为 ONNX 格式，部署在 **Unitree G1** 人形机器人的车载计算单元上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真中每种偏好设置均进行了 **1,000 次并行测试**。
    *   涵盖了从极端偏好（1.0, 0.0）到平衡偏好（0.5, 0.5）的多种组合。
*   **充分性评价**：实验设计较为充分。不仅包含了仿真中的定量分析（超体积、帕累托曲线），还进行了消融实验验证 PCI 模块的必要性。
*   **客观性**：通过实物机器人（Unitree G1）的对比实验验证了算法的“从仿真到现实（Sim-to-Real）”的迁移能力，并展示了在任务执行过程中实时切换偏好的动态响应。

### 6. 主要结论与发现
*   **单一策略多行为**：PCHC 成功实现了用一个模型覆盖整个帕累托前沿，机器人能根据输入的 $\lambda$ 展现出从“保守稳定”到“激进高效”的连续行为变化。
*   **性能优于基准**：在相同能耗下，PCHC 比基准方法具有更强的鲁棒性；在相同鲁棒性下，能耗更低。
*   **PCI 模块的有效性**：基于 Beta 分布的 MoE 路由机制在超体积和稀疏度指标上均优于传统的门控网络，证明了其在多目标插值上的优越性。
*   **实时切换**：实验证明机器人可以在运动过程中瞬时切换偏好向量，且不会导致系统崩溃或失稳。

### 7. 优点
*   **灵活性高**：解决了人形机器人部署后行为不可调的痛点，允许用户根据实际场景（如电量不足时选节能模式）实时干预。
*   **架构创新**：巧妙地将 Beta 分布与 MoE 结合，解决了多目标偏好在神经网络中难以有效映射到低级电机控制的问题。
*   **实战性强**：在 Unitree G1 硬件上完成了闭环验证，证明了算法的实用价值。

### 8. 不足与局限
*   **目标数量限制**：目前实验主要集中在**两个**冲突目标的平衡（2D 帕累托前沿）。虽然理论上可扩展，但随着目标增加，Beta 分布需扩展为高维 Dirichlet 分布，计算复杂度和训练难度会显著增加。
*   **传感器依赖**：目前仅基于本体感受（Proprioceptive）信息，属于“盲视”控制，尚未集成视觉或激光雷达等外部感知信息。
*   **专家数量超参数**：文中设定专家数量为 5 个，虽然效果良好，但缺乏关于专家数量对复杂任务影响的深度讨论。

（完）
