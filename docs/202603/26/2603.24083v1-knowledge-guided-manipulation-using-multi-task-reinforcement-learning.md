---
title: Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning
title_zh: 基于多任务强化学习的知识引导操纵
authors: "Aditya Narendra, Mukhammadrizo Maribjonov, Dmitry Makarov, Dmitry Yudin, Aleksandr Panov"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24083v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 统一感知、知识与策略的机器人操作
tldr: 本文提出KG-M3PO框架，旨在解决部分可观测环境下的多任务机器人操作难题。该方法将第一视角视觉与在线3D场景图结合，通过动态关系机制捕捉空间、包含及赋能关系，并利用图神经编码器在强化学习目标下进行端到端训练。通过融合视觉、本体感知、语言和图特征，该智能体在处理遮挡、干扰和布局变化时表现出更强的鲁棒性，显著提升了操作任务的成功率、采样效率和泛化能力。
selection_source: fresh_fetch
motivation: 针对部分可观测环境下多任务机器人操作中感知与决策脱节的问题，探索如何利用结构化知识引导强化学习以提升控制性能。
method: 提出一种结合3D场景图与强化学习的框架，通过动态更新的关系图和端到端训练的图编码器，将多模态信息编码进统一潜空间进行决策。
result: 实验证明该方法在任务成功率、采样效率以及对新物体和未知场景布局的泛化能力上均显著优于现有强基准模型。
conclusion: 结构化且持续维护的世界知识是实现可扩展、通用化机器人操作的强大归纳偏置，能有效使关系表示与控制任务对齐。
---

## 摘要
本文介绍了基于知识图谱的大规模多任务基于模型的策略优化（KG-M3PO），这是一个在部分可观测环境下统一感知、知识与策略的多任务机器人操纵框架。该方法通过在线 3D 场景图增强了第一视角视觉，将开放词汇检测落地为度量化的关系表示。动态关系机制在每一步更新空间、包含和示能（affordance）边，并且通过强化学习（RL）目标端到端地训练图神经编码器，使关系特征直接由控制性能塑造。多种观测模态（视觉、本体感受、语言和基于图的模态）被编码到一个共享的潜空间中，强化学习智能体在此基础上运行以驱动控制循环。策略以轻量级图查询以及视觉和本体感受输入为条件，为决策提供紧凑且具有语义信息的决策状态。在包含遮挡、干扰物和布局变化的一系列操纵任务上的实验表明，该方法相比强基线模型取得了持续的提升：以知识为条件的智能体实现了更高的成功率、更好的样本效率，以及对新物体和未见场景配置更强的泛化能力。这些结果支持了这样一个前提：结构化且持续维护的世界知识是实现可扩展、可泛化操纵的强大归纳偏置。当知识模块参与强化学习计算图时，关系表示与控制相对齐，从而在部分可观测性下实现稳健的长程行为。

## Abstract
This paper introduces Knowledge Graph based Massively Multi-task Model-based Policy Optimization (KG-M3PO), a framework for multi-task robotic manipulation in partially observable settings that unifies Perception, Knowledge, and Policy. The method augments egocentric vision with an online 3D scene graph that grounds open-vocabulary detections into a metric, relational representation. A dynamic-relation mechanism updates spatial, containment, and affordance edges at every step, and a graph neural encoder is trained end-to-end through the RL objective so that relational features are shaped directly by control performance. Multiple observation modalities (visual, proprioceptive, linguistic, and graph-based) are encoded into a shared latent space, upon which the RL agent operates to drive the control loop. The policy conditions on lightweight graph queries alongside visual and proprioceptive inputs, yielding a compact, semantically informed state for decision making.   Experiments on a suite of manipulation tasks with occlusions, distractors, and layout shifts demonstrate consistent gains over strong baselines: the knowledge-conditioned agent achieves higher success rates, improved sample efficiency, and stronger generalization to novel objects and unseen scene configurations. These results support the premise that structured, continuously maintained world knowledge is a powerful inductive bias for scalable, generalizable manipulation: when the knowledge module participates in the RL computation graph, relational representations align with control, enabling robust long-horizon behavior under partial observability.

---

## 论文详细总结（自动生成）

以下是对论文《Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：在非结构化和日常环境中，机器人操纵任务通常面临**部分可观测性（Partial Observability）**。物体可能被遮挡、放置在容器内或被移动。
*   **研究动机**：
    *   纯反应式策略（像素到转矩）容易产生状态混淆，且在长程任务中难以进行信用分配。
    *   传统的基于模型的几何方法过于脆弱，难以适应多任务和不同机器人形态。
    *   需要一种能够整合**结构化世界知识**、**持久化状态估计**和**多任务泛化能力**的统一框架。

### 2. 方法论
论文提出了 **KG-M3PO**（知识引导的大规模多任务基于模型的策略优化）框架，其核心思想是将感知、知识和策略统一在强化学习（RL）的计算图中。
*   **核心思想**：利用动态更新的 3D 场景图（3DSG）作为智能体的“信念状态”，并通过 RL 目标函数端到端地训练图编码器。
*   **关键技术细节**：
    *   **在线场景图构建**：使用 BBQ 算法从第一视角 RGB-D 流中提取物体节点（包含 CLIP 嵌入和 3D 边界框）和关系边（如“在...之上”、“在...之后”、“包含”）。
    *   **动态关系机制**：每 10 个步骤局部刷新受影响的子图，确保空间和语义关系的实时性。
    *   **多模态融合**：将视觉特征（CNN 提取）、知识特征（GNN 提取）、语言目标（CLIP 编码）和本体感知数据拼接成统一的潜空间向量 $s_t$。
    *   **端到端训练**：图神经网络（GNN）编码器 $GNN_\phi$ 的参数直接通过 RL 的策略梯度进行更新。这意味着关系表示是根据“是否有助于提高控制性能”来优化的，而非仅仅是几何重建。
    *   **控制骨干**：基于 M3PO（一种结合了模型预测和策略优化的混合算法），支持大规模并行训练。

### 3. 实验设计
*   **实验环境**：基于 NVIDIA Isaac Lab (Isaac Sim) 构建的物理仿真环境，支持 GPU 加速的并行渲染和动力学模拟。
*   **任务设置**：
    *   **全可观测（FO）任务**：抓取方块、放置方块、开柜子。
    *   **部分可观测（PO）任务**：抓取墙后的物体、移除遮挡物后抓取并放置物体。
*   **机器人平台**：Franka Emika Panda (7-DoF) 和 UR5 (6-DoF)。
*   **对比基准（Baselines）**：
    *   **无知识图谱基准**：PPO, SAC, DreamerV3, TD-MPC2, IMPALA, M3PO（仅相机输入）。
    *   **多任务基准**：MT-PPO, MT-SAC 等。

### 4. 资源与算力
*   **硬件配置**：单台工作站，配备 **NVIDIA RTX 5070 Ti (16 GB VRAM)** 和 64 GB RAM。
*   **并行规模**：利用 Isaac Lab 的平铺渲染 API，在单个进程中同时运行 **1024 个并行环境**。
*   **训练时长**：
    *   标准 M3PO（仅相机）：4亿步训练约需 0.61 GPU-天。
    *   KG-M3PO（带知识图谱）：4亿步训练约需 **0.79 GPU-天**（由于增加了图处理开销，速度慢约 30%，但采样效率更高）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种核心任务，每种任务都在两种机器人平台上进行了验证。
*   **消融实验**：
    *   **输入模态对比**：对比了“仅相机”与“相机+知识图谱”的效果。
    *   **噪声鲁棒性实验**：对场景图的边界框中心、尺寸和旋转添加了不同程度的噪声（Low/High），验证了 GNN 编码器的容错能力。
*   **评价指标**：采用了归一化得分（0-1000）、成功率和曲线下面积（AUC）。
*   **充分性评价**：实验设计较为全面，通过跨机器人、跨任务难度（FO vs PO）以及噪声测试，客观地证明了结构化知识在复杂操纵中的必要性。

### 6. 主要结论与发现
*   **知识图谱是关键**：在部分可观测任务中，仅靠相机的基准模型几乎无法完成任务，而 KG-M3PO 能够通过持久化的图结构记住被遮挡物体的位置。
*   **提升采样效率**：即使在全可观测任务中，引入知识图谱也能显著加快收敛速度，因为图结构提供了明确的实体关系引导。
*   **端到端优势**：将 GNN 纳入 RL 损失函数进行训练，比使用预训练或分离的图特征效果更好，因为特征表示与控制目标实现了对齐。
*   **鲁棒性**：策略对中等程度的几何感知误差（如检测漂移）具有较强的防御能力。

### 7. 优点与亮点
*   **统一框架**：成功将高层的语义/关系推理（3DSG）与底层的连续控制（RL）无缝结合。
*   **动态更新**：提出的在线关系更新机制解决了静态场景图容易过时的问题。
*   **高效训练**：在消费级 GPU 上实现了大规模并行训练，证明了该框架的实用性。

### 8. 不足与局限
*   **Sim-to-Real 缺失**：所有实验均在仿真中完成，尚未在真实物理机器人上进行验证，真实环境中的感知噪声和延迟可能更复杂。
*   **计算开销**：虽然采样效率高，但每一步的图更新和 GNN 推理增加了额外的计算负担。
*   **动态物体限制**：对于高速运动或拓扑结构频繁变化的物体（如流体、绳索），目前的场景图表示可能难以应对。
*   **依赖感知质量**：虽然有噪声消融，但如果底层物体检测器完全失效，整个知识引导机制将崩溃。

（完）
