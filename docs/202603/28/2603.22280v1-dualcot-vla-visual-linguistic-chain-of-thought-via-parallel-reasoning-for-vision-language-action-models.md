---
title: "DualCoT-VLA: Visual-Linguistic Chain of Thought via Parallel Reasoning for Vision-Language-Action Models"
title_zh: DualCoT-VLA：基于并行推理的视觉-语言思维链，用于视觉-语言-动作模型
authors: "Zhide Zhong, Junfeng Li, Junjie He, Haodong Yan, Xin Gong, Guanyi Zhao, Yingjie Cai, Jiantao Gao, Xu Yan, Bingbing Liu, Yingcong Chen, Liuqing Yang, Haoang Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22280v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 用于复杂多步任务和精确操作的VLA模型
tldr: 针对视觉-语言-动作（VLA）模型在复杂任务中逻辑规划和空间感知不足的问题，本文提出DualCoT-VLA。该方法结合了用于低层空间理解的视觉CoT和用于高层任务规划的语言CoT，并通过并行推理机制将自回归推理转变为单步前向推理，显著降低了推理延迟并减少了误差累积，在多个基准测试和真实场景中实现了领先性能。
selection_source: fresh_fetch
motivation: 现有CoT-VLA模型因依赖单模态推理而无法兼顾视觉细节与逻辑规划，且自回归解码导致推理延迟高且易产生累积误差。
method: 提出集成视觉与语言双重思维链的DualCoT方法，并利用可学习查询标记实现并行推理以替代传统的自回归过程。
result: 在LIBERO、RoboCasa GR1基准测试及真实机器人平台上均达到了当前最先进的性能水平。
conclusion: DualCoT-VLA通过多模态并行推理机制，有效解决了VLA模型在复杂任务中的感知与效率瓶颈。
---

## 摘要
视觉-语言-动作 (VLA) 模型将视觉观测和语言指令直接映射为机器人动作。虽然标准 VLA 模型在简单任务中表现出色，但在需要逻辑规划的复杂多步任务以及需要细粒度空间感知的精确操作中往往面临挑战。最近的研究引入了思维链 (CoT) 推理，赋予 VLA 模型“先思后行”的能力。然而，目前的基于 CoT 的 VLA 模型面临两个关键局限：1) 由于依赖孤立的单模态 CoT，无法同时捕捉底层视觉细节和高层逻辑规划；2) 逐步自回归解码导致的高推理延迟和复合误差。为了解决这些局限，我们提出了 DualCoT-VLA，这是一种具有并行推理机制的 VLA 模型视觉-语言 CoT 方法。为了实现全面的多模态推理，我们的方法集成了用于底层空间理解的视觉 CoT 和用于高层任务规划的语言 CoT。此外，为了克服延迟瓶颈，我们引入了一种并行 CoT 机制，该机制结合了两组可学习的查询标记，将自回归推理转变为单步前向推理。广泛的实验表明，我们的 DualCoT-VLA 在 LIBERO 和 RoboCasa GR1 基准测试以及真实世界平台中均达到了最先进的性能。

## Abstract
Vision-Language-Action (VLA) models map visual observations and language instructions directly to robotic actions. While effective for simple tasks, standard VLA models often struggle with complex, multi-step tasks requiring logical planning, as well as precise manipulations demanding fine-grained spatial perception. Recent efforts have incorporated Chain-of-Thought (CoT) reasoning to endow VLA models with a ``thinking before acting'' capability. However, current CoT-based VLA models face two critical limitations: 1) an inability to simultaneously capture low-level visual details and high-level logical planning due to their reliance on isolated, single-modal CoT; 2) high inference latency with compounding errors caused by step-by-step autoregressive decoding. To address these limitations, we propose DualCoT-VLA, a visual-linguistic CoT method for VLA models with a parallel reasoning mechanism. To achieve comprehensive multi-modal reasoning, our method integrates a visual CoT for low-level spatial understanding and a linguistic CoT for high-level task planning. Furthermore, to overcome the latency bottleneck, we introduce a parallel CoT mechanism that incorporates two sets of learnable query tokens, shifting autoregressive reasoning to single-step forward reasoning. Extensive experiments demonstrate that our DualCoT-VLA achieves state-of-the-art performance on the LIBERO and RoboCasa GR1 benchmarks, as well as in real-world platforms.

---

## 论文详细总结（自动生成）

### DualCoT-VLA 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的视觉-语言-动作（VLA）模型通常采用端到端的直接映射，在处理需要**长程逻辑规划**和**精细空间感知**的复杂任务时表现不佳。虽然近期引入的思维链（CoT）技术有所改善，但仍面临两大瓶颈：
*   **模态孤立**：现有的 CoT 方法通常仅限于单一模态（纯文本或纯视觉），导致模型无法同时兼顾高层逻辑规划与底层 3D 空间细节。
*   **推理延迟与误差累积**：主流 CoT 依赖自回归（AR）逐 token 解码，不仅推理速度极慢（难以满足机器人实时控制需求），且容易产生一步错步步错的复合误差。

#### 2. 方法论：核心思想与关键技术
DualCoT-VLA 提出了一种**并行隐式双模态推理机制**，核心思想是将推理过程从显式的文本生成转变为连续潜在空间中的单步前向推理。
*   **并行查询机制**：引入两组可学习的查询标记（Query Tokens）——视觉 CoT 查询（16个）和语言 CoT 查询（4个）。这些标记与视觉观测、语言指令一起输入 VLM 主干网络（Qwen3-VL-4B），通过一次前向传播提取推理特征。
*   **视觉 CoT（几何蒸馏）**：利用冻结的视觉基础模型（Depth Anything 3）作为教师，通过交叉注意力投影器，将 VLM 的视觉查询状态与密集的 3D 空间特征对齐，赋予模型底层空间感知能力。
*   **语言 CoT（步骤级监督）**：利用冻结的轻量级 LLM（Qwen3-0.6B）作为解码器，监督 VLM 的语言查询状态生成人类可读的任务规划文本，迫使模型在潜在空间中内化逻辑规划。
*   **动作生成**：将富含推理信息的隐藏状态输入基于 **Flow-Matching** 的扩散 Transformer（DiT）动作头，预测连续的动作序列。
*   **推理优化**：在推理阶段，丢弃辅助的教师模块，仅靠 VLM 一次前向计算即可完成“思考”与“决策”。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个任务套件，评估长程任务能力。
    *   **RoboCasa GR1**：涉及 29 自由度灵巧手的复杂桌面任务（24 类任务），评估精细操作能力。
    *   **真实世界**：基于 AgileX Cobot（Mobile ALOHA 架构）的双臂机器人，设计了简单（面包）、中等（方块）、困难（水果）三种难度的任务。
*   **对比方法**：
    *   **非 CoT 模型**：Diffusion Policy, OpenVLA, $\pi_0$, PD-VLA, GR00T 系列。
    *   **自回归 CoT 模型**：CoT-VLA, ThinkAct, DeepThinkVLA, Fast-ThinkAct, LaRA-VLA。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA H100 GPU** 进行训练和推理延迟测试。
*   **参数设置**：LIBERO 训练学习率为 2.5e-5，Batch Size 为 48；RoboCasa 训练学习率为 3e-5，Batch Size 为 256。
*   **具体算力消耗**：文中未明确给出总训练时长或使用的 GPU 总数，但强调了推理延迟的优化（从自回归的 3178.5ms 降至并行的 83.2ms）。

#### 5. 实验数量与充分性
*   **实验规模**：在 LIBERO 上进行了 500 个回合的评估，在 RoboCasa 上进行了 50 个回合的评估，实机实验每个任务 25 次尝试。
*   **消融实验**：专门对比了“无 CoT”、“仅视觉 CoT”、“仅语言 CoT”以及“双 CoT”的效果，验证了双模态的互补性。
*   **客观性**：通过可视化“视觉探针”（重建深度图）和“语言解码”（生成文本）证明了隐式表征确实包含了有效的推理信息，而非简单的过拟合。实验覆盖了仿真与实机、简单与复杂、低维与高维动作空间，具有较强的说服力。

#### 6. 主要结论与发现
*   **性能领先**：DualCoT-VLA 在 LIBERO（平均成功率 98.8%）和 RoboCasa GR1（平均成功率 55.1%）上均刷新了 SOTA 记录。
*   **效率飞跃**：相比自回归 CoT 模型，推理速度提升了约 **38 倍**，总延迟仅为 83.2ms，完全满足实时控制要求。
*   **模态协同**：视觉 CoT 显著提升了空间感知任务（如 Spatial 类别），而语言 CoT 则对长程逻辑任务（如 Long 类别）至关重要。

#### 7. 优点
*   **架构优雅**：通过并行 Query 巧妙解决了 CoT 推理速度慢的顽疾，实现了“思考”与“行动”的同步。
*   **隐式推理**：避免了显式文本生成的冗余和 token 级累积误差，同时保留了通过探针进行解释的能力。
*   **泛化性强**：在灵巧手操作和真实世界非结构化环境中表现出良好的鲁棒性。

#### 8. 不足与局限
*   **依赖教师模型**：训练过程高度依赖高质量的视觉（DA3）和语言（LLM）教师模型，若教师模型存在偏差，可能影响 VLA 的上限。
*   **数据标注成本**：虽然使用了自动生成技术，但高质量的语言 CoT 标注（状态跟踪、空间定位、动作制定）在扩展到更大规模数据集时仍具挑战。
*   **应用限制**：目前主要集中在桌面操作任务，对于更广阔的移动导航或动态环境下的表现尚待验证。

（完）
