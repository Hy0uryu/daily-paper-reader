---
title: "SG-VLA: Learning Spatially-Grounded Vision-Language-Action Models for Mobile Manipulation"
title_zh: SG-VLA：学习用于移动操作的空间基准视觉-语言-动作模型
authors: "Ruisen Tu, Arth Shukla, Sohyun Yoo, Xuanlin Li, Junxi Li, Jianwen Xie, Hao Su, Zhuowen Tu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22760v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 移动操作中底盘运动、手臂关节和夹持器的协调控制
tldr: 针对移动操作任务中视觉语言动作（VLA）模型在复杂环境表现不佳的问题，本文提出SG-VLA框架。该框架通过引入多视图RGB、深度信息及时间序列增强感知，并利用辅助任务（如位姿估计、分割等）协同训练，强化了模型对空间几何和全局布局的理解。在13维动作空间的家庭重排任务中，该方法显著提升了抓取、放置等操作的成功率，优于传统模仿学习。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理涉及全局布局和精细几何的复杂移动操作任务时，由于缺乏空间感知能力，导致控制性能不足。
method: 提出一种结合多模态输入与辅助任务协同训练的框架，通过重建机器人位姿、抓取启发式和分割掩码等信号来增强空间表征。
result: 在包含抓取、放置、开关门等操作的家庭重排任务中，SG-VLA的表现大幅优于直接模仿学习方法。
conclusion: 空间接地通过辅助学习和多模态融合，是提升通用家务机器人VLA模型性能的有效途径。
---

## 摘要
视觉-语言-动作（VLA）模型在机器人控制方面展现出巨大潜力，但在复杂的家庭环境中的表现仍不理想。移动操作需要对全局场景布局、细粒度几何结构以及高维连续动作进行推理，这使得标准的模仿学习难以胜任。我们引入了一个学习空间基准 VLA 模型的框架，通过辅助任务协同训练和多模态输入增强来强化感知与表示。我们的方法解决了控制 13 维动作空间的挑战，该空间涉及底盘运动、机械臂关节运动和夹持器动作的协同。为了丰富空间理解，该模型整合了多视图 RGB 观测、深度线索和短期时间历史，提供了全局场景结构和局部操作上下文的视角。为了提高表示质量，我们协同训练了辅助解码器，从共享的视觉-语言特征中重建可解释的中间信号——包括机器人全局位置、关节配置、抓取示能性（affordances）、目标物体相对位姿以及分割掩码。这些目标提供了密集监督，促使骨干网络学习到具有空间基准和操作感知的潜表示。通过对家庭重排任务的广泛评估，我们的方法在拾取、放置、打开和关闭操作中均实现了一致的改进，显著优于直接模仿学习。我们的研究结果表明，通过辅助学习和多模态学习实现空间基准，为将 VLA 模型扩展到通用家用机器人提供了一个强有力的方向。

## Abstract
Vision-Language-Action (VLA) models show promise for robotic control, yet performance in complex household environments remains sub-optimal. Mobile manipulation requires reasoning about global scene layout, fine-grained geometry, and high-dimensional continuous actions, making standard imitation learning insufficient. We introduce a framework for learning spatially-grounded VLA models that strengthens perception and representation through auxiliary task co-training and multi-modal input enhancement. Our method addresses the challenge of controlling a 13-dimensional action space involving coordinated base motion, arm articulation, and gripper actuation. To enrich spatial understanding, the model incorporates multi-view RGB observations, depth cues, and short temporal history, providing perspectives of both global scene structure and local manipulation context. To improve representation quality, we co-train auxiliary decoders that reconstruct interpretable intermediate signals - including global robot position, joint configurations, grasp affordances, target-object relative pose, and segmentation masks - from shared visual-language features. These objectives provide dense supervision that encourages the backbone to develop spatially grounded, manipulation-aware latent representations. Through extensive evaluation on home rearrangement tasks, our approach achieves consistent improvements across picking, placing, opening, and closing operations, substantially outperforming direct imitation learning. Our findings suggest that spatial grounding through auxiliary and multi-modal learning provides a strong direction for scaling VLA models toward general-purpose domestic robots.

---

## 论文详细总结（自动生成）

以下是对论文《SG-VLA: Learning Spatially-Grounded Vision-Language-Action Models for Mobile Manipulation》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在简单的桌面操作中表现出色，但在复杂的**移动操作（Mobile Manipulation）**任务中（如家庭环境）表现不佳。
*   **研究背景**：移动操作要求机器人同时处理全局场景布局、精细物体几何以及高维连续动作（底盘+机械臂+夹持器）。传统的直接模仿学习（Imitation Learning）难以提供足够的监督信号，导致模型缺乏对空间几何的深度理解。

### 2. 方法论
SG-VLA 框架的核心思想是通过**增强感知输入**和**引入辅助任务协同训练**来建立“空间基准（Spatial Grounding）”。
*   **多模态输入增强**：
    *   **多视图**：同时使用头部（Head）和手部（Hand）摄像头的 RGB 图像。
    *   **深度信息**：引入归一化后的深度图，提供显式的几何距离信息。
    *   **时间上下文**：包含过去 4 个时间步的历史观测。
*   **辅助任务协同训练**：在共享的 VLM 骨干网络上添加多个辅助解码器，重建以下信号：
    *   **全局位置**（2D 坐标）、**抓取成功率**（二分类）、**物体位姿**（3D 位置+四元数）、**关节配置**（12 维关节角）、**语义分割掩码**（128x128 目标物体掩码）。
*   **三阶段渐进式训练流程**：
    1.  **解码器适配阶段**：冻结 VLM 骨干，仅训练辅助解码器，避免随机初始化的解码器产生噪声梯度破坏预训练权重。
    2.  **联合微调阶段**：开启全量梯度流，让辅助任务损失引导骨干网络学习空间感知的表示。
    3.  **动作头训练阶段**（可选）：冻结 VLM，单独训练基于流匹配（Flow Matching）的连续动作专家模型。
*   **动作空间**：控制 13 维动作向量，包括底盘位姿（3D）、躯干高度（1D）、机械臂关节（7D）和夹持器状态（2D）。

### 3. 实验设计
*   **数据集与场景**：使用 **ManiSkill-HAB** 仿真基准，包含三个长程家庭重排任务：TidyHouse（房屋整理）、PrepareGroceries（准备杂货）和 SetTable（摆放餐桌）。
*   **任务分解**：涵盖拾取（Pick）、放置（Place）、打开（Open）和关闭（Close）四类基本操作。
*   **Benchmark 与对比方法**：
    *   **基准模型**：OpenVLA（约 7B 参数）。
    *   **对比实验**：对比了直接模仿学习（无辅助任务）、不同输入模态组合（单视图 vs 多视图，RGB vs RGB-D）以及不同辅助任务组合的效果。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：使用 Adam 优化器，学习率恒定为 2e-5，全局 Batch Size 为 512。
*   **时长**：分割任务训练 5 个 Epoch，其他任务训练 10 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：使用了 4.4 万个情节（Episodes），共计 140 万次状态转换（Transitions）。
*   **消融实验**：针对输入模态（表 2）、辅助任务类型（表 3/4）、训练策略（表 3）和动作生成模式（表 5）进行了详尽的消融研究。
*   **评价指标**：每个任务运行 30 个情节并计算平均成功率。实验设计较为客观，通过控制变量法清晰地展示了深度信息和辅助任务对性能的提升。

### 6. 主要结论与发现
*   **空间信息至关重要**：引入多视图和深度信息后，模型成功率从极低水平大幅提升（OpenVLA 基础版 4% -> 增强版 32%）。
*   **辅助任务的增益**：在相同架构下，加入辅助任务协同训练使平均成功率从 60% 提升至 **73%**。其中，**关节位置（qpos）预测**和**分割掩码**对操作性能的提升最为显著。
*   **渐进式训练的必要性**：直接协同训练会导致性能下降（60% -> 51%），而渐进式训练能有效利用辅助信号。
*   **动作头权衡**：流匹配动作头在精细的“拾取”任务中表现更好，但在需要大范围移动的“打开”任务中表现不如离散动作标记。

### 7. 优点
*   **轻量化且高效**：基于 1.3B 参数的模型（Qwen2.5-0.5B 骨干）实现了优于更大规模模型的性能。
*   **可解释性强**：通过辅助任务（如分割和位姿估计），可以直观观察模型是否“理解”了当前场景。
*   **训练策略稳健**：提出的三阶段训练法解决了多任务学习中梯度干扰的常见痛点。

### 8. 不足与局限
*   **仿真与现实差距**：实验完全在 ManiSkill-HAB 仿真环境中进行，尚未在真实物理机器人上验证其迁移能力（Sim-to-Real）。
*   **时间上下文的负面影响**：实验发现加入历史动作反而导致性能下降（60% -> 49%），论文推测是模型未能有效整合时间信息，这反映了模型在处理动态序列上的局限。
*   **任务覆盖面**：虽然涵盖了家庭常见任务，但对于更复杂的长程逻辑推理（如多步骤规划）探讨较少。

（完）
