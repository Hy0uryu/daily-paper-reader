---
title: Gaze-Regularized Vision-Language-Action Models for Robotic Manipulation
title_zh: 用于机器人操作的注视正则化视觉-语言-动作模型
authors: "Anupam Pani, Yanchao Yang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23202v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 用于引导机器人感知与操作的视线正则化VLA模型
tldr: 针对视觉-语言-动作（VLA）模型在精细操作任务中缺乏主动视觉注意力分配的问题，本文提出了一种注视正则化训练框架。该方法通过KL散度将人类注视热图转化为补丁级分布，引导模型内部注意力对齐人类视觉模式。实验表明，该方法在不增加推理开销的前提下，显著提升了操作任务的成功率、收敛速度和鲁棒性，同时增强了模型的可解释性。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理精细机器人操作时，缺乏像人类注视那样能够编码意图和规划的主动视觉注意力机制。
method: 利用KL散度将时间聚合的人类注视热图作为正则化项，引导Transformer模型的内部注意力机制关注任务相关特征。
result: "在多个机器人操作基准测试中实现了4-12%的性能提升，并表现出更快的收敛速度和更强的环境鲁棒性。"
conclusion: 引入人类感知先验能有效加速机器人学习并提升任务表现，同时无需额外硬件即可增强系统的可解释性。
---

## 摘要
尽管视觉-语言-动作（VLA）模型取得了进展，但由于当前模型缺乏主动视觉注意力分配机制，机器人操作在处理细粒度任务时仍面临困难。人类注视自然地编码了意图、规划和执行模式，为引导机器人感知提供了强大的监督信号。我们引入了一种注视正则化训练框架，在不改变架构或增加推理开销的情况下，将 VLA 模型的内部注意力与人类视觉模式对齐。我们的方法将时间聚合的注视热图转换为 patch 级分布，并通过 KL 散度对 Transformer 的注意力进行正则化，在保持部署效率的同时，建立了对任务相关特征的归纳偏置。当集成到现有的 VLA 架构中时，我们的方法在各项操作基准测试中实现了 4-12% 的提升。注视正则化模型以更少的训练步数达到了同等性能，并在光照变化和传感器噪声下保持了鲁棒性。除了性能指标外，学习到的注意力模式还产生了反映人类策略的可解释可视化结果，增强了对机器人系统的信任。此外，我们的框架不需要眼动追踪设备，并可直接应用于现有数据集。这些结果表明，人类感知先验可以显著加速机器人学习，同时提高任务性能和系统可解释性。

## Abstract
Despite advances in Vision-Language-Action (VLA) models, robotic manipulation struggles with fine-grained tasks because current models lack mechanisms for active visual attention allocation. Human gaze naturally encodes intent, planning, and execution patterns -- offering a powerful supervisory signal for guiding robot perception. We introduce a gaze-regularized training framework that aligns VLA models' internal attention with human visual patterns without architectural modifications or inference-time overhead. Our method transforms temporally aggregated gaze heatmaps into patch-level distributions and regularizes the transformer's attention through KL divergence, creating an inductive bias toward task-relevant features while preserving deployment efficiency. When integrated into existing VLA architectures, our approach yields 4-12% improvements across manipulation benchmarks. The gaze-regularized models reach equivalent performance with fewer training steps and maintain robustness under lighting variations and sensor noise. Beyond performance metrics, the learned attention patterns produce interpretable visualizations that mirror human strategies, enhancing trust in robotic systems. Moreover, our framework requires no eye-tracking equipment and applies directly to existing datasets. These results demonstrate that human perceptual priors can significantly accelerate robot learning while improving both task performance and system interpretability.

---

## 论文详细总结（自动生成）

### 论文总结：用于机器人操作的注视正则化视觉-语言-动作模型 (Gaze-Regularized VLA)

#### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型在处理精细机器人操作时，通常采用“被动视觉理解”模式，即模型必须同时学习“看哪里”和“如何做”。这种并行的学习负担导致训练效率低下、收敛缓慢，且在复杂环境下缺乏鲁棒性。
*   **核心问题**：如何利用人类在操作任务中的视觉注意力（注视点）作为先验知识，引导模型主动关注任务相关区域，从而提升学习效率和操作精度。
*   **整体含义**：本文提出了一种注视正则化框架，通过在训练阶段将人类注视模式引入 VLA 模型的内部注意力机制，使模型具备类似人类的“主动感知”能力，且不增加推理成本。

#### 2. 论文提出的方法论
*   **核心思想**：将人类注视点作为一种归纳偏置（Inductive Bias），通过正则化手段强制模型在处理视觉信息时，其注意力分布与人类注视分布对齐。
*   **关键技术细节**：
    *   **注视先验生成**：由于机器人数据集通常缺少眼动数据，作者使用预训练的注视预测模型（如 GLC 网络）在视频序列上生成合成注视热图（Heatmaps）。
    *   **时间聚合**：为了捕捉人类注视的预测性和稳定性，对连续帧的热图进行加权平均，生成时间聚合的注视分布。
    *   **补丁级分布转换**：将连续的热图投影到与 Transformer 视觉 Token 相同的网格上，转化为离散的概率分布 $G$。
    *   **注意力正则化**：提取 VLA 模型最后一层 Transformer 的视觉-语言交叉注意力分布 $S$，计算其与注视分布 $G$ 之间的 **KL 散度（Kullback-Leibler Divergence）**。
*   **损失函数**：总损失 $L_{total} = L_{action} + \lambda \cdot D_{KL}(G \parallel S)$。其中 $L_{action}$ 是标准的动作预测损失（如流匹配损失），$\lambda$ 是正则化系数。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO Suite**：包括 Spatial（空间关系）、Object（物体识别）、Goal（目标导向）和 LIBERO-10（多任务泛化）。
    *   **ALOHA-Sim**：用于验证跨域泛化能力（如方块移动、插销任务）。
    *   **真实世界机器人**：在实体机器人上进行了抓取、放置等短时程和长时程任务验证。
*   **Benchmark 与对比方法**：
    *   **基准模型**：以 Pi-0 和 OpenVLA 为基础架构。
    *   **对比实验**：对比了无注视正则化的基准模型（Baseline）、不同正则化强度（$\lambda$）的模型、以及使用不同注视预测器（如 DeepGaze）或均匀分布的变体。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练规模**：实验记录了在 10k、20k、30k 及 40k 训练步数下的性能表现，显示出注视正则化在早期训练阶段（如 20k 步）即表现出明显的加速收敛优势。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了仿真环境下的 4 个主要基准测试集、跨架构迁移实验（Pi-0 到 OpenVLA）、真实机器人部署实验，以及针对光照变化、传感器噪声的鲁棒性测试。
*   **消融实验**：对正则化系数 $\lambda$ 进行了敏感度分析，并对比了合成注视与真实人类眼动数据的对齐度（IoU 验证）。
*   **充分性评价**：实验设计较为全面，不仅关注成功率，还深入探讨了收敛速度、鲁棒性和可解释性，实验结果具有较强的说服力。

#### 6. 论文的主要结论与发现
*   **性能提升**：注视正则化使 VLA 模型在 LIBERO 基准上的成功率提升了 4-12%，在 LIBERO-Spatial 任务中从 85.9% 提升至 95.5%。
*   **加速收敛**：正则化模型在较少的训练步数下即可达到基准模型的最终性能，显著提高了样本效率。
*   **鲁棒性增强**：在光照变化和相机噪声环境下，注视正则化模型表现出更强的稳定性，因为它学会了关注语义稳定的特征（如物体边界）。
*   **可解释性**：可视化结果显示，正则化后的模型注意力更集中于任务关键区域（如目标物体及其周围），而非背景杂质。

#### 7. 优点
*   **推理零开销**：注视引导仅发生在训练阶段，推理时不需要眼动仪或注视预测模型，保持了部署的轻量化。
*   **架构无关性**：该方法作为一种辅助损失函数，可以轻松集成到 Pi-0、OpenVLA 等多种 Transformer 架构中。
*   **无需额外标注**：利用预训练模型生成合成注视，解决了机器人领域缺乏大规模眼动标注数据的难题。

#### 8. 不足与局限
*   **合成注视的偏差**：虽然实验证明合成注视有效，但其本质上是人类视觉模式的近似，可能无法完全捕捉专家在极复杂任务中的细微意图。
*   **任务依赖性**：对于某些不依赖视觉引导的任务（如纯触觉或盲操作），该方法的提升可能有限。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于其他研究者评估训练成本。
*   **真实世界规模**：虽然包含真实机器人实验，但任务种类（3 种）相对仿真环境较少，在大规模真实场景下的泛化能力仍有待进一步验证。

（完）
