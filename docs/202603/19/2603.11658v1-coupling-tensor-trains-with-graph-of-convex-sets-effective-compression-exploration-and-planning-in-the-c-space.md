---
title: "Coupling Tensor Trains with Graph of Convex Sets: Effective Compression, Exploration, and Planning in the C-Space"
title_zh: 将张量列与凸集图耦合：在配置空间中进行有效的压缩、探索与规划
authors: "Gerhard Reinerth, Riddhiman Laha, Marcello Romano"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11658v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 通用机器人任务高维配置空间中的运动规划
tldr: 本研究提出TANGO框架，旨在解决高维配置空间中运动规划的计算难题。针对凸集图（GCS）规划器依赖预定义凸区域的局限性，TANGO利用张量列（Tensor Train）分解对可行配置空间进行压缩近似，从而快速识别任务相关区域。通过将这些区域嵌入类GCS结构，实现了兼顾系统约束与环境复杂性的高效几何感知规划，在机器人仿真和实物实验中展现了优异的压缩性能和轨迹质量。
selection_source: fresh_fetch
motivation: 传统的凸集图规划器在处理高维配置空间时，往往难以预先获取复杂环境下的凸区域定义。
method: 利用张量列分解对可行配置空间进行压缩近似，并将其识别的区域集成到结构化图优化框架中。
result: 在平面和真实机器人上的仿真研究验证了该方法在空间压缩效率和轨迹生成质量上的显著提升。
conclusion: TANGO通过耦合张量压缩与图优化，为未来机器人系统提供了一种更具表现力和可扩展性的配置空间表示与规划方法。
---

## 摘要
我们提出了 TANGO（张量与图优化），这是一种新型运动规划框架，它将基于张量的压缩与结构化图优化相结合，以实现高效且可扩展的轨迹生成。虽然诸如凸集图 (GCS) 等基于优化的规划器为生成平滑、最优轨迹提供了强大的工具，但它们通常依赖于高维配置空间的预定义凸表征——这一要求对于通用机器人任务通常是难以处理的。TANGO 通过使用张量列 (Tensor Train) 分解以压缩形式近似可行配置空间，从而进一步实现了对任务相关区域的快速发现和估计。随后，这些区域被嵌入到类 GCS 结构中，从而实现既符合系统约束又考虑环境复杂性的几何感知运动规划。通过将基于张量的压缩与结构化图推理相结合，TANGO 实现了高效的几何感知运动规划，并为未来机器人系统中更具表现力和可扩展性的配置空间表示奠定了基础。在平面和真实机器人上进行的严格仿真研究证实了我们在有效压缩和更高质量轨迹方面的论点。

## Abstract
We present TANGO (Tensor ANd Graph Optimization), a novel motion planning framework that integrates tensor-based compression with structured graph optimization to enable efficient and scalable trajectory generation. While optimization-based planners such as the Graph of Convex Sets (GCS) offer powerful tools for generating smooth, optimal trajectories, they typically rely on a predefined convex characterization of the high-dimensional configuration space-a requirement that is often intractable for general robotic tasks. TANGO builds further by using Tensor Train decomposition to approximate the feasible configuration space in a compressed form, enabling rapid discovery and estimation of task-relevant regions. These regions are then embedded into a GCS-like structure, allowing for geometry-aware motion planning that respects both system constraints and environmental complexity. By coupling tensor-based compression with structured graph reasoning, TANGO enables efficient, geometry-aware motion planning and lays the groundwork for more expressive and scalable representations of configuration space in future robotic systems. Rigorous simulation studies on planar and real robots reinforce our claims of effective compression and higher quality trajectories.

---

## 论文详细总结（自动生成）

### 论文总结：TANGO (Tensor ANd Graph Optimization) 框架

#### 1. 核心问题与研究动机
在高维配置空间（C-Space）中进行机器人运动规划是一个极具挑战性的问题。虽然**凸集图（GCS）**等基于优化的规划器能生成平滑且最优的轨迹，但它们高度依赖于对配置空间的预定义凸区域划分，而这在复杂或高维任务中往往难以实现。传统的采样规划器（如 RRT）在处理约束流形时效率低下，且缺乏对空间覆盖率的估计。
**研究动机：** 提出一种能够高效压缩高维空间、自动识别任务相关可行区域，并利用凸优化进行几何感知规划的新框架。

#### 2. 方法论：核心思想与技术细节
TANGO 框架将**张量列（Tensor Train, TT）分解**与**凸集图（GCS）优化**耦合，分为两个主要阶段：

*   **张量列预处理（压缩与采样）：**
    *   **PDF 近似：** 使用任务特定指标（如 Yoshikawa 可操作度或黎曼度量）构建概率密度函数（PDF），描述配置空间中“好”的区域。
    *   **TT-Cross 算法：** 利用 TT-Cross 贪婪采样策略，将高维 PDF 近似为低秩张量核心的乘积，从而绕过“维度灾难”。
    *   **逆向采样：** 同时构建逆向 PDF 以识别障碍物区域。通过 TT-Sample 快速抽取可行配置点和障碍物点。
*   **凸区域发现与图规划：**
    *   **区域生长（IRIS）：** 以 TT 抽取的优质配置为种子点，利用 IRIS 算法通过半正定规划（SDP）迭代生长出不含障碍物的最大凸多胞形。
    *   **GCS 构建：** 检测凸区域之间的交集，将其作为图的边。
    *   **最短路径问题（SPP）：** 在 GCS 上通过凸优化求解连接起点和终点的几何路径，确保路径完全位于安全凸区域内。

#### 3. 实验设计
*   **实验场景：**
    1.  **3-DoF 平面机械臂：** 用于可视化分析 TT 近似 PDF 的准确性以及凸区域的生成过程。
    2.  **4-DoF Franka Panda 机器人：** 验证高维扩展性（锁定 7 轴中的 3 个轴以降低复杂度）。
*   **Benchmark（基准方法）：** 传统的 **RRT 规划器**。
*   **对比指标：** 轨迹执行时间、PDF 得分（反映可操作度/避开奇异点的能力）、路径点数量（反映平滑度）、内存占用及计算耗时。

#### 4. 资源与算力
*   **算力说明：** 文中提到为了加速计算使用了**多进程（Multiprocessing）**并行处理 IRIS 区域生长和凸集剪枝。
*   **硬件细节：** 论文**未明确列出**具体的 GPU 或 CPU 型号、内存容量及总训练/计算时长。

#### 5. 实验数量与充分性
*   **实验规模：** 在 Panda 机器人实验中，随机选择了 **100 组** 具有较大关节空间距离的起点-终点对进行测试。在内存和时间足迹分析中，进行了 **15 次** 重复试验以获取均值和标准差。
*   **充分性评价：** 实验设计较为客观，对比了采样效率和轨迹质量。但目前主要集中在 4 自由度，对于全 7 自由度或更高维度的复杂动态环境实验尚显不足。

#### 6. 主要结论与发现
*   **压缩效率：** TT 分解能显著降低内存占用（参数量从 $4.36 \times 10^4$ 降至 $7.11 \times 10^3$），且计算速度提升约 5 倍。
*   **轨迹质量：** 相比 RRT，TANGO 生成的路径点更少（通常仅需 3-4 个路标点），轨迹更平滑，且平均可操作度（PDF Score）更高，能有效避开运动学奇异点。
*   **安全性：** 通过 IRIS 生成的凸区域提供了明确的安全保证，确保路径远离关节极限和奇异位姿。

#### 7. 优点与亮点
*   **创新性耦合：** 首次将张量压缩技术（TT）与现代凸优化规划（GCS）结合，解决了 GCS 寻找初始凸区域的难题。
*   **几何感知：** 引入黎曼度量引导采样，使规划器天生具备避开奇异点和优化机械臂姿态的能力。
*   **高效性：** TT 分解使高维空间的表征从指数级增长转变为线性增长，极大地提升了存储和检索效率。

#### 8. 不足与局限
*   **环境障碍物处理：** 目前主要针对配置空间（C-Space）内的约束（如关节限位、奇异点），对于工作空间（Task-Space）中的动态障碍物，文中仅提到可以通过 SDF 扩展，但未在实验中深入展示。
*   **维度限制：** 实验最高仅展示到 4-DoF，对于 7-DoF 及以上全维度机器人的实时性表现仍需进一步验证。
*   **参数敏感性：** 框架依赖于多个超参数（如 $\gamma$ 缩放因子、采样数量等），这些参数目前主要靠经验设定。

（完）
