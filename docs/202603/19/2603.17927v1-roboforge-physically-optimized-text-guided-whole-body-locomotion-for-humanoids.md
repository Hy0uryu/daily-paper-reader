---
title: "RoboForge: Physically Optimized Text-guided Whole-Body Locomotion for Humanoids"
title_zh: RoboForge：针对类人机器人的物理优化文本引导全身运动
authors: "Xichen Yuan, Zhe Li, Bofan Lyu, Kuangji Zuo, Yanshuo Lu, Gen Li, Jianfei Yang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17927v1"
tags: ["query:hr-rob"]
score: 10.0
evidence: 人形机器人全身运动与运动规划
tldr: 本研究针对人形机器人文本引导运动生成中物理可行性差的问题，提出了RoboForge框架。该框架通过物理合理性优化（PP-Opt）模块，将扩散生成模型与物理控制器双向耦合：前向优化控制器以抑制漂浮和穿模等伪影，后向利用物理优化数据微调生成器。这种自改进循环使生成器学习到符合物理规律的潜空间，实现了无需显式重定向的高质量全身运动控制。
selection_source: fresh_fetch
motivation: 现有的文本到运动生成方法在迁移至人形机器人时，常因忽略物理约束而导致运动失真、接触错误及动力学不匹配。
method: 引入PP-Opt模块作为耦合接口，通过前向策略蒸馏和后向生成器微调，构建了一个物理优化的双向自改进闭环。
result: 在Unitree G1人形机器人上的实验证明，该方法在跟踪精度、成功率和物理稳定性方面均优于传统的显式重定向基准。
conclusion: RoboForge通过将物理合理性深度集成到潜空间驱动的流水线中，为实现可部署的文本引导人形机器人智能提供了有效方案。
---

## 摘要
虽然生成模型在根据文本生成类人动作方面已变得非常有效，但将这些动作转移到类人机器人上进行物理执行仍然具有挑战性。现有的流程通常受限于重定向（retargeting），其运动学质量往往因物理不可行性、接触转换错误以及获取真实世界动力学数据的高昂成本而受损。我们提出了一个统一的潜空间驱动框架，通过一种无需重定向且经过物理优化的流程，连接了自然语言与类人机器人的全身运动。我们的核心见解并非将生成与控制视为独立阶段，而是在物理约束下将两者进行双向耦合。我们引入了一个物理合理性优化（PP-Opt）模块作为耦合接口。在正向路径中，PP-Opt 通过以合理性为中心的奖励函数来改进教师-学生蒸馏策略，从而抑制诸如漂浮、滑步和穿模等伪影。在反向路径中，它将经过奖励优化的仿真展开（rollouts）转换为高质量的显式运动数据，用于微调运动生成器，使其趋向于更符合物理合理性的潜空间分布。这种双向设计形成了一个自我改进的循环：生成器学习具有物理基础的潜空间，而控制器则学习执行具有动力学完整性的潜空间条件行为。在宇树（Unitree）G1 类人机器人上进行的大量实验表明，我们的双向优化提高了跟踪精度和成功率。在 IsaacLab 和 MuJoCo 平台上，这种隐式潜空间驱动流程在精度和稳定性方面均一致优于传统的显式重定向基准。通过将基于扩散的运动生成与物理合理性优化相结合，我们的框架为实现可部署的文本引导类人机器人智能提供了一条切实可行的路径。

## Abstract
While generative models have become effective at producing human-like motions from text, transferring these motions to humanoid robots for physical execution remains challenging. Existing pipelines are often limited by retargeting, where kinematic quality is undermined by physical infeasibility, contact-transition errors, and the high cost of real-world dynamical data. We present a unified latent-driven framework that bridges natural language and whole-body humanoid locomotion through a retarget-free, physics-optimized pipeline. Rather than treating generation and control as separate stages, our key insight is to couple them bidirectionally under physical constraints.We introduce a Physical Plausibility Optimization (PP-Opt) module as the coupling interface. In the forward direction, PP-Opt refines a teacher-student distillation policy with a plausibility-centric reward to suppress artifacts such as floating, skating, and penetration. In the backward direction, it converts reward-optimized simulation rollouts into high-quality explicit motion data, which is used to fine-tune the motion generator toward a more physically plausible latent distribution. This bidirectional design forms a self-improving cycle: the generator learns a physically grounded latent space, while the controller learns to execute latent-conditioned behaviors with dynamical integrity.Extensive experiments on the Unitree G1 humanoid show that our bidirectional optimization improves tracking accuracy and success rates. Across IsaacLab and MuJoCo, the implicit latent-driven pipeline consistently outperforms conventional explicit retargeting baselines in both precision and stability. By coupling diffusion-based motion generation with physical plausibility optimization, our framework provides a practical path toward deployable text-guided humanoid intelligence.

---

## 论文详细总结（自动生成）

以下是对论文《RoboForge: Physically Optimized Text-guided Whole-Body Locomotion for Humanoids》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管扩散模型在“文本生成动作”领域取得了巨大进步，但生成的动作往往只具备视觉上的流畅性，缺乏物理可行性。
*   **核心痛点**：
    1.  **物理伪影**：生成的动作常出现滑步（Foot Skating）、地面穿模（Penetration）和空中漂浮（Floating）。
    2.  **重定向瓶颈**：传统的“生成-重定向-跟踪”流程在不同动力学模型转换时会产生累积误差，导致机器人失稳。
    3.  **数据稀缺**：高质量的人形机器人真实动力学数据极难获取。
*   **整体含义**：本研究提出了 **RoboForge**，一个统一的潜空间驱动框架，通过双向耦合生成模型与物理控制器，实现了无需显式重定向、物理优化的文本引导全身运动控制。

### 2. 方法论
*   **核心思想**：将“生成”与“控制”视为一个互惠的闭环系统。通过物理合理性优化（PP-Opt）模块，既让控制器学会修正不合理的动作，也让生成器学会生成更符合物理规律的潜变量。
*   **关键技术细节**：
    *   **前向优化（控制器改进）**：使用强化学习优化跟踪策略。引入物理合理性奖励函数 $J(\xi)$，专门惩罚滑步、漂浮和穿模行为，使控制器在执行潜变量指令时更具动力学完整性。
    *   **后向微调（生成器改进）**：将仿真中经过物理优化后的高质量运动序列（通过 MPJPE 阈值过滤掉失败案例）反馈给运动生成器（MG），对其进行微调，使其潜空间分布向“物理可行区域”偏移。
    *   **潜空间驱动流程**：采用教师-学生蒸馏架构（结合 DAgger 算法）。学生策略直接以运动潜变量（Latent）作为输入，跳过了传统的显式重定向步骤，减少了运动学漂移。

### 3. 实验设计
*   **机器人平台**：Unitree G1 人形机器人。
*   **数据集**：HumanML3D 和 KIT-ML（主流的文本-动作对齐数据集）。
*   **Benchmark 与对比方法**：
    *   **生成对比**：对比了原始的 MLD（运动潜空间扩散模型）与经过 PP-Opt 优化后的版本。
    *   **控制对比**：对比了“隐式潜空间接口（Ours）”与“显式重定向接口（如 GMR）”。
*   **实验场景**：在 **IsaacLab**（用于大规模并行训练）和 **MuJoCo**（用于跨仿真器验证和 Sim-to-Sim 迁移）中进行测试，涵盖拳击、武术、跳跃等复杂全身动作。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量或训练总时长。通常此类涉及大规模强化学习和扩散模型微调的研究需要高性能计算集群（如 NVIDIA A100 或 H100 级别）。

### 5. 实验数量与充分性
*   **实验组设置**：
    *   **消融实验**：分别验证了 PP-Opt 对生成质量和跟踪稳定性的提升。
    *   **迭代验证**：进行了 1-3 轮“生成-执行-微调”循环的对比，证明了性能随迭代次数的提升。
    *   **跨平台验证**：在两个不同的物理引擎（IsaacLab 和 MuJoCo）中进行评估，确保了结果的客观性。
*   **充分性评价**：实验设计较为全面，不仅关注传统的生成指标（FID, R-Precision），还深入分析了物理指标（滑步距离、穿模深度等），实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **物理合理性显著提升**：PP-Opt 成功将穿模现象降至接近 0，并大幅减少了漂浮和滑步伪影。
*   **闭环优化的优越性**：通过多轮迭代，生成器生成的潜变量质量持续提高，FID 指标显著下降，证明了“物理反馈微调生成器”的有效性。
*   **隐式接口更鲁棒**：直接跟踪潜变量（Latent-driven）比传统的显式重定向跟踪具有更低的跟踪误差（MPJPE）和更高的任务成功率。

### 7. 优点
*   **双向耦合机制**：打破了生成与控制的孤立状态，实现了生成模型与物理环境的深度对齐。
*   **无需重定向（Retarget-free）**：简化了部署流程，有效解决了人形机器人控制中因人体结构差异导致的运动失真问题。
*   **自改进循环**：框架具备自我进化的潜力，通过仿真数据不断精炼生成器的物理常识。

### 8. 不足与局限
*   **环境单一性**：实验主要集中在平坦地面，尚未验证在非结构化地形（如楼梯、斜坡）或复杂障碍物环境下的表现。
*   **动作筛选偏差**：为了保证物理稳定性，过滤掉了一些非地面接触的动作（如游泳、交互类动作），这可能限制了模型处理极端多样化动作的能力。
*   **实时性挑战**：论文未详细讨论扩散模型在实时部署时的计算延迟问题，这对于高频机器人控制至关重要。

（完）
