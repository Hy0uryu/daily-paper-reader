---
title: "Chasing Autonomy: Dynamic Retargeting and Control Guided RL for Performant and Controllable Humanoid Running"
title_zh: 追求自主性：用于高性能且可控人形机器人奔跑的动态重定向与控制引导强化学习
authors: "Zachary Olkin, William D. Compton, Ryan M. Bena, Aaron D. Ames"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25902v1"
tags: ["query:hr-rob"]
score: 9.0
evidence: 用于动态类人跑步的强化学习控制器
tldr: 本研究针对人形机器人高速奔跑中的自主性难题，提出了一种结合动态重定向与控制引导强化学习（RL）的框架。通过对单个人类动作演示进行带硬约束的优化，生成周期性参考库，并采用目标条件与控制引导的奖励结构进行训练。该方法在Unitree G1机器人上实现了3.3 m/s的奔跑速度，并成功集成感知与规划系统，实现了户外避障自主航行，显著提升了人形机器人的运动性能与可控性。
selection_source: fresh_fetch
motivation: 传统的模仿学习控制器通常局限于单一动作回放，难以满足人形机器人长距离自主运动的需求。
method: 提出一种通过硬约束优化动态重定向人类动作的流水线，并结合目标条件和控制引导的强化学习奖励结构。
result: 在Unitree G1机器人上实现了高达3.3 m/s的奔跑速度，并完成了数百米的户外实地测试及自主避障任务。
conclusion: 动态优化的参考运动与控制引导的强化学习相结合，是实现高性能且可控的人形机器人奔跑的关键。
---

## 摘要
人形机器人有望实现像人类一样的运动，包括快速且动态的奔跑。近年来，能够模仿人类动作的强化学习（RL）控制器因其能生成极具动态性的行为而广受欢迎，但它们通常局限于单一动作的回放，这阻碍了其在长时程和自主运动中的部署。在本文中，我们提出了一套流程，通过带有硬约束的优化程序对人类动作进行动态重定向，从而从单个人类演示中生成改进的周期性参考库。随后，我们研究了参考运动和奖励结构对参考速度及指令速度跟踪的影响，并得出结论：跟踪动态优化后的人类数据，并结合目标条件（goal-conditioned）和控制引导（control-guided）的奖励，可以获得最佳性能。我们在硬件上部署了该策略，在 Unitree G1 机器人上实现了高达 3.3 m/s 的奔跑速度，并在真实环境中行进了数百米，展示了其速度和耐力。此外，为了展示运动的可控性，我们将该控制器应用于完整的感知与规划自主系统栈中，实现了室外奔跑时的避障。

## Abstract
Humanoid robots have the promise of locomoting like humans, including fast and dynamic running. Recently, reinforcement learning (RL) controllers that can mimic human motions have become popular as they can generate very dynamic behaviors, but they are often restricted to single motion play-back which hinders their deployment in long duration and autonomous locomotion. In this paper, we present a pipeline to dynamically retarget human motions through an optimization routine with hard constraints to generate improved periodic reference libraries from a single human demonstration. We then study the effect of both the reference motion and the reward structure on the reference and commanded velocity tracking, concluding that a goal-conditioned and control-guided reward which tracks dynamically optimized human data results in the best performance. We deploy the policy on hardware, demonstrating its speed and endurance by achieving running speeds of up to 3.3 m/s on a Unitree G1 robot and traversing hundreds of meters in real-world environments. Additionally, to demonstrate the controllability of the locomotion, we use the controller in a full perception and planning autonomy stack for obstacle avoidance while running outdoors.

---

## 论文详细总结（自动生成）

以下是对论文《Chasing Autonomy: Dynamic Retargeting and Control Guided RL for Performant and Controllable Humanoid Running》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注人形机器人（Humanoid Robots）在高速奔跑中的**自主性（Autonomy）**与**可控性（Controllability）**。
*   **研究动机**：虽然强化学习（RL）在模仿人类动作方面取得了进展，但现有方法通常局限于单一动作的回放（Play-back），难以在长距离、多变的自主导航任务中灵活调整速度和方向。
*   **核心问题**：如何生成既具备人类运动美感（Dynamic），又能精准响应高层指令（Controllable）的奔跑控制器，并将其无缝集成到包含感知和规划的自主系统栈中。

### 2. 方法论：核心思想与关键技术
论文提出了一套从单个人类动作演示到硬件自主部署的完整流水线：
*   **动态重定向（Dynamic Retargeting）**：
    *   使用**多点打靶法（Multiple Shooting）**轨迹优化，将人类运动数据（来自 LAFAN 数据集）映射到机器人动力学模型上。
    *   引入硬约束（如周期性、动力学可行性、足端不打滑），从单个人类动作片段生成一个包含不同速度（1.2 m/s 至 3.6 m/s）的**参考动作库**。
*   **控制引导的强化学习（CLF-RL）**：
    *   **奖励结构**：放弃传统的单一模仿奖励，引入**控制李雅普诺夫函数（CLF）**引导的奖励。通过定义李雅普诺夫函数 $V$ 及其衰减条件（$\dot{V} + \lambda V \leq 0$），在奖励函数中直接嵌入稳定性判据。
    *   **目标条件（Goal Conditioning）**：在训练中加入速度指令（$v_x, v_y, \dot{\psi}$）作为输入，使策略能够根据指令在动作库中插值并调整步态。
*   **训练细节**：采用非对称 Actor-Critic 架构，在 IsaacLab 环境中进行大规模并行训练，并加入领域随机化（摩擦力、质量、电机增益等）以增强鲁棒性。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：IsaacSim（用于训练）和 MuJoCo（用于 Sim-to-Sim 验证，确保不依赖特定物理引擎）。
    *   **硬件平台**：Unitree G1 人形机器人。
*   **对比方法（Benchmark）**：
    *   **参考运动对比**：运动学重定向 vs. 运动学优化 vs. 动态优化（有/无人类数据）。
    *   **奖励结构对比**：传统 Mimic（模仿）奖励 vs. CLF-RL 奖励。
    *   **速度跟踪对比**：仅运动学调整 vs. 目标条件奖励（Goal Conditioning）。
*   **硬件测试**：室内跑步机测试（极限速度）、室外长距离奔跑（耐力）、自主避障（集成感知栈）。

### 4. 资源与算力
*   **软件工具**：IsaacLab/IsaacSim (GPU 加速仿真), RSL-RL (PPO 算法实现), CasADi/IPOPT (轨迹优化)。
*   **算力说明**：文中提到使用了“GPU 加速的物理仿真”，但**未明确给出具体的 GPU 型号、数量及训练总时长**。这是此类基于 IsaacGym/IsaacLab 论文的常见情况（通常涉及数千个并行环境）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了两组主要的消融实验：一组针对参考运动质量（8 种组合），另一组针对速度跟踪性能（多种速度梯度）。
    *   仿真中每组实验包含 200 个随机环境的统计平均值。
    *   硬件实验包括超过 250 米的连续室外奔跑和 3.3 m/s 的高速测试。
*   **充分性评价**：实验设计非常充分且客观。通过 Sim-to-Sim 验证和真实的室外自主避障实验，证明了该方法不仅在实验室环境下有效，在复杂的真实世界感知-规划循环中同样稳健。

### 6. 主要结论与发现
*   **动态优化至关重要**：经过动力学约束优化的参考运动比单纯的运动学重定向更能提升 RL 的跟踪精度。
*   **CLF 奖励优于模仿奖励**：引入控制理论中的稳定性判据（CLF）能显著降低跟踪误差，使运动更稳定。
*   **自主性集成**：通过目标条件训练，机器人可以像“单积分器”一样被高层规划器（如 MPC+CBF）调用，实现了人形机器人在奔跑状态下的自主避障。
*   **性能突破**：在 Unitree G1 上实现了 3.3 m/s 的奔跑速度，这是该平台目前公开报道的领先水平。

### 7. 优点
*   **理论与学习的结合**：成功将经典的控制理论（CLF、轨迹优化）与现代强化学习融合，解决了 RL 奖励函数难调优的问题。
*   **数据效率**：仅需单个人类动作片段即可生成覆盖全速度范围的动作库。
*   **实战导向**：不局限于“动作模仿”，而是真正实现了可用于自主导航的“运动原语”。

### 8. 不足与局限
*   **模型依赖**：动态重定向阶段需要准确的机器人混合动力学模型（Hybrid System Model），这增加了系统建模的复杂度。
*   **步态切换**：虽然提到了从行走（Walk）到奔跑（Run）的过渡，但对于更复杂地形（如楼梯、碎石路）的适应性尚未在本文中深入探讨。
*   **硬件限制**：3.3 m/s 的速度受限于 G1 机器人的电机功率和发热，而非算法本身的上限。

（完）
