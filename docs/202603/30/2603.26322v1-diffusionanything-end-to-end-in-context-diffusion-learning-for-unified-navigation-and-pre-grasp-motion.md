---
title: "DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion"
title_zh: DiffusionAnything：用于统一导航与抓取前运动的端到端上下文扩散学习
authors: "Iana Zhura, Yara Mahmoud, Jeffrin Sam, Hung Khang Nguyen, Didar Seyidov, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26322v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 统一的导航与预抓取运动规划
tldr: 本研究提出 DiffusionAnything，一种统一的端到端图像空间扩散策略，旨在解决机器人导航与抓取前运动规划中的效率与泛化难题。该模型仅需极少自监督数据，通过多尺度 FiLM 调节和轨迹对齐深度预测，实现了从米级导航到厘米级操作的跨尺度任务处理。其轻量化设计支持纯 RGB 输入和实时部署，在未知场景中展现出卓越的零样本泛化能力。
selection_source: fresh_fetch
motivation: 针对现有视觉动作模型计算资源消耗大、在未知场景中零样本泛化能力差以及导航与操作任务难以统一的问题。
method: 采用多尺度 FiLM 调节、轨迹对齐深度预测及自监督注意力机制，构建了一个轻量级的端到端图像空间扩散策略模型。
result: 该模型仅需 2.0 GB 显存即可实现 10 Hz 的实时推理，并在无需深度传感器的情况下于新场景中表现出极强的泛化性能。
conclusion: DiffusionAnything 证明了利用极少量自监督数据即可实现跨尺度的统一运动规划，为具身智能在资源受限设备上的部署提供了高效方案。
---

## 摘要
直接从视觉高效预测运动规划仍然是机器人领域的一个基本挑战，通常规划需要明确的目标说明和特定任务的设计。最近的视觉-语言-动作（VLA）模型直接从视觉输入推断动作，但需要巨大的计算资源、大量的训练数据，且在陌生场景中无法实现零样本泛化。我们提出了一种统一的图像空间扩散策略，通过多尺度特征调制，仅需每个任务 5 分钟的自监督数据，即可处理米级导航和厘米级操作。该框架由三项关键创新驱动：(1) 基于任务模式、深度尺度和空间注意力的多尺度 FiLM 条件化，使单一模型能够实现适应任务的行为；(2) 轨迹对齐的深度预测将度量 3D 推理集中在生成的路径点上；(3) 来自 AnyTraverse 的自监督注意力实现了无需视觉语言模型和深度传感器的目标导向推理。该模型纯粹基于 RGB 输入运行（2.0 GB 内存，10 Hz），在实现对新场景的鲁棒零样本泛化的同时，仍适用于机载部署。

## Abstract
Efficiently predicting motion plans directly from vision remains a fundamental challenge in robotics, where planning typically requires explicit goal specification and task-specific design. Recent vision-language-action (VLA) models infer actions directly from visual input but demand massive computational resources, extensive training data, and fail zero-shot in novel scenes. We present a unified image-space diffusion policy handling both meter-scale navigation and centimeter-scale manipulation via multi-scale feature modulation, with only 5 minutes of self-supervised data per task. Three key innovations drive the framework: (1) Multi-scale FiLM conditioning on task mode, depth scale, and spatial attention enables task-appropriate behavior in a single model; (2) trajectory-aligned depth prediction focuses metric 3D reasoning along generated waypoints; (3) self-supervised attention from AnyTraverse enables goal-directed inference without vision-language models and depth sensors. Operating purely from RGB input (2.0 GB memory, 10 Hz), the model achieves robust zero-shot generalization to novel scenes while remaining suitable for onboard deployment.

---

## 论文详细总结（自动生成）

以下是对论文《DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在单一轻量化模型中统一处理机器人**米级（meter-scale）导航**与**厘米级（centimeter-scale）抓取前运动规划**，并解决现有视觉-语言-动作（VLA）模型计算开销大、数据需求高以及在陌生场景中零样本泛化能力差的问题。
*   **研究动机**：传统的机器人架构将导航与操作分离，导致累积误差和高延迟；而大型 VLA 模型（如 RT-2）虽然具备泛化性，但推理速度慢且难以部署在实时控制回路中。本文旨在通过扩散策略（Diffusion Policy）实现一种高效、端到端且具备空间感知能力的统一框架。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用基于图像空间的扩散策略，通过多尺度特征调制（FiLM）将任务上下文（模式、尺度、注意力）注入统一的 UNet 架构中。
*   **关键技术细节**：
    *   **多尺度 FiLM 调节**：在 UNet 编码器的四个尺度上应用特征线性调制。上下文向量 $c$ 包含任务模式（导航/抓取）、深度尺度（米/厘米）和空间注意力图。这使得模型能根据任务动态调整关注区域（如导航关注地面，抓取关注物体）。
    *   **轨迹对齐的深度推理**：模型不预测全量深度图，而是仅预测生成的轨迹路径点上的深度值。这种设计显著降低了计算量，同时保证了避障和精确接近所需的度量 3D 信息。
    *   **自监督注意力引导**：利用 AnyTraverse 框架自动生成可通行区域和物体注意力图作为监督信号，无需人工标注或视觉语言模型（VLM）参与推理。
    *   **目标学习**：支持自主目标选择（基于场景上下文）和用户指定目标（通过参考图像特征匹配），实现零样本目标导向行为。

### 3. 实验设计
*   **实验场景**：包括室内走廊（导航场景）和带有各种物体的桌面（抓取前操作场景）。
*   **机器人平台**：Unitree G1 人形机器人（配备 RGB 摄像头，处理 7-DOF 手臂操作和双足行走）。
*   **对比基准（Benchmarks）**：
    *   **导航**：对比了 NoMaD（一种在大规模数据集上训练的视觉导航策略）。
    *   **操作/泛化**：对比了 NVIDIA 的 GR00T (n1.5 和 n1.6) 模型。
*   **评估指标**：成功率（SR）、避障间距（Clearance）、目标距离误差（Dist）。

### 4. 资源与算力
*   **训练算力**：使用单张 **NVIDIA RTX 4090 GPU**。
    *   **预训练**：在 20 分钟视频数据上训练 50 个 epoch，耗时约 2 小时。
    *   **任务适配**：针对新任务仅需 5 分钟新数据，微调 10 个 epoch 仅需 15-20 分钟。
*   **推理资源**：仅需 **2.0 GB 显存**，推理频率达到 **10 Hz**，适合机载实时部署。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了探索导航、目标导向导航和抓取前规划三大类任务。
*   **充分性评价**：
    *   **客观性**：通过定量数据展示了模型在不同尺度下的精度差异（抓取精度比导航高 6 倍），证明了尺度调节的有效性。
    *   **泛化性测试**：在完全未见的场景中进行了零样本测试，并与 GR00T 进行了对比，展示了其在分布外（OOD）场景下的鲁棒性。
    *   **局限性**：虽然实验涵盖了核心任务，但总训练数据量较小（分钟级），这既是该方法的亮点（高效），也可能意味着在极度复杂的长程任务中仍需进一步验证。

### 6. 主要结论与发现
*   **统一性**：单一模型能够同时处理跨度巨大的空间任务（米级 vs 厘米级）。
*   **精度表现**：预抓取任务实现了 4.71 cm 的平均误差，而导航任务实现了 100% 的避障成功率。
*   **泛化优势**：相比于端到端的 VLA 模型（如 GR00T 在新场景中表现出系统性失败），引入显式几何推理（可通行性、空间注意力）的模型在零样本迁移中表现更优。
*   **高效性**：证明了通过极少量的自监督数据（每个任务 5 分钟）即可实现高性能的机器人策略学习。

### 7. 优点
*   **极低的数据依赖**：无需人工演示或遥操作，完全依赖自监督生成的标签。
*   **轻量化与实时性**：2GB 显存占用和 10Hz 的速度使其非常适合人形机器人的机载计算。
*   **多尺度适应**：通过 FiLM 巧妙解决了导航的“粗糙”与操作的“精细”之间的矛盾。
*   **无需深度传感器**：仅靠单目 RGB 即可实现度量级别的 3D 推理。

### 8. 不足与局限
*   **监督依赖**：高度依赖 AnyTraverse 框架生成的伪标签质量，若 AnyTraverse 失效，模型性能会受限。
*   **缺乏力反馈**：目前仅限于运动规划（Pre-grasp），未涉及实际接触后的力控或触觉反馈。
*   **任务复杂度**：实验主要集中在接近物体和基础导航，对于复杂的多步协同操作（如开门并穿过）尚待验证。
*   **成功率空间**：预抓取成功率为 70.6%，在严苛的工业或家庭应用中仍有提升空间。

（完）
