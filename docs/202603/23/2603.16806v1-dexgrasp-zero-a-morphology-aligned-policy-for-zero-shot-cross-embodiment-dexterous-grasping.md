---
title: "DexGrasp-Zero: A Morphology-Aligned Policy for Zero-Shot Cross-Embodiment Dexterous Grasping"
title_zh: DexGrasp-Zero：一种用于零样本跨具身灵巧抓取的形态对齐策略
authors: "Yuliang Wu, Yanhan Lin, WengKit Lao, Yuhao Lin, Yi-Lin Wei, Wei-Shi Zheng, Ancong Wu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16806v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 跨具身灵巧抓取与形态对齐策略
tldr: "针对多样化灵巧手硬件的跨机器人抓取难题，本文提出 DexGrasp-Zero 框架。该方法通过形态对齐的图表示将不同手的运动学关键点映射到统一节点，并利用形态对齐图卷积网络（MAGCN）结合物理属性注入机制，实现了对不同手部结构和物理约束的自适应。实验表明，该策略在未见过的灵巧手上实现了 85% 的零样本抓取成功率，显著优于现有方法，并成功应用于真实世界机器人。"
selection_source: fresh_fetch
motivation: 现有的跨机器人抓取方法在处理异构运动学和物理约束时容易产生误差，难以在不同灵巧手之间实现高效的零样本迁移。
method: 提出一种形态对齐的图表示方法及 MAGCN 网络，通过将物理约束注入图特征，实现跨形态的结构与语义对齐。
result: "在仿真中对未见过的硬件实现了 85% 的零样本抓取成功率，在真实世界的三种机器人平台上也达到了 82% 的平均成功率。"
conclusion: DexGrasp-Zero 证明了通过形态对齐和物理属性注入，可以学习到具备强泛化能力的通用灵巧抓取技能。
---

## 摘要
为了满足日益多样化的灵巧手硬件需求，开发一种无需冗余重新学习即可实现零样本跨具身抓取的策略至关重要。由于异构手的运动学和物理约束，跨具身对齐具有挑战性。现有方法通常预测中间运动目标并将其重定向到每个具身，这可能会引入误差并违反特定具身的限制，从而阻碍在不同手之间的迁移。为了克服这些局限性，我们提出了 DexGrasp-Zero，这是一种从多样化具身中学习通用抓取技能的策略，能够实现向未知手的零样本迁移。我们首先引入了一种形态对齐的图表示，将每只手的运动学关键点映射到具有解剖学基础的节点，并为每个节点配备三轴正交运动原语，从而实现不同形态之间的结构和语义对齐。依托这种基于图的表示，我们设计了一个形态对齐图卷积网络（MAGCN）来对图进行编码以进行策略学习。MAGCN 结合了物理属性注入机制，将特定手的物理约束融合到图特征中，从而能够对不同的连杆长度和驱动限制进行自适应补偿，实现精确且稳定的抓取。我们在 YCB 数据集上进行的广泛仿真评估表明，我们的策略在四种异构手（Allegro、Shadow、Schunk、Ability）上联合训练，在未知硬件（LEAP、Inspire）上实现了 85% 的零样本成功率，比现有最先进方法高出 59.5%。真实世界实验进一步在三个机器人平台（LEAP、Inspire、Revo2）上评估了我们的策略，在未知物体上实现了 82% 的平均成功率。

## Abstract
To meet the demands of increasingly diverse dexterous hand hardware, it is crucial to develop a policy that enables zero-shot cross-embodiment grasping without redundant re-learning. Cross-embodiment alignment is challenging due to heterogeneous hand kinematics and physical constraints. Existing approaches typically predict intermediate motion targets and retarget them to each embodiment, which may introduce errors and violate embodiment-specific limits, hindering transfer across diverse hands. To overcome these limitations, we propose \textit{DexGrasp-Zero}, a policy that learns universal grasping skills from diverse embodiments, enabling zero-shot transfer to unseen hands. We first introduce a morphology-aligned graph representation that maps each hand's kinematic keypoints to anatomically grounded nodes and equips each node with tri-axial orthogonal motion primitives, enabling structural and semantic alignment across different morphologies. Relying on this graph-based representation, we design a \textit{Morphology-Aligned Graph Convolutional Network} (MAGCN) to encode the graph for policy learning. MAGCN incorporates a \textit{Physical Property Injection} mechanism that fuses hand-specific physical constraints into the graph features, enabling adaptive compensation for varying link lengths and actuation limits for precise and stable grasping. Our extensive simulation evaluations on the YCB dataset demonstrate that our policy, jointly trained on four heterogeneous hands (Allegro, Shadow, Schunk, Ability), achieves an 85\% zero-shot success rate on unseen hardware (LEAP, Inspire), outperforming the state-of-the-art method by 59.5\%. Real-world experiments further evaluate our policy on three robot platforms (LEAP, Inspire, Revo2), achieving an 82\% average success rate on unseen objects.

---

## 论文详细总结（自动生成）

### DexGrasp-Zero：一种用于零样本跨具身灵巧抓取的形态对齐策略

#### 1. 论文的核心问题与整体含义
随着灵巧手硬件的快速迭代，不同设备在运动学结构（自由度、连杆长度）和物理约束上存在显著差异。现有的强化学习（RL）策略通常绑定于特定硬件，难以直接迁移。传统跨具身方法多采用“中间运动目标+重定向（Retargeting）”的模式，但这容易违反特定硬件的物理约束，导致动作不可行。
**DexGrasp-Zero** 的核心目标是开发一种通用的抓取策略，通过形态对齐的图表示和物理属性注入，实现从已知灵巧手到未知灵巧手的**零样本（Zero-Shot）**迁移，无需针对新硬件进行重新训练或微调。

#### 2. 论文提出的方法论
该方法通过构建一个统一的感知与控制框架来消除形态差异：
*   **形态对齐的图表示（Morphology-Aligned Graph Representation）：** 将不同灵巧手的运动学结构抽象为具有解剖学意义的图。节点分为六类语义类型（指尖、远端、中端、近端、掌骨、手腕），边表示运动链连接。
*   **通用运动原语空间（Hand-Agnostic Motion-Primitive Space）：** 为每个图节点定义了三个正交的生物力学原语：屈曲（Flexion）、外展（Abduction）和轴向旋转（Axial Rotation）。这使得不同形态的手在语义层面上拥有统一的动作空间。
*   **形态对齐图卷积网络（MAGCN）：** 
    *   **物理属性注入（Physical Property Injection）：** 从 URDF 文件中提取连杆长度、关节限制、阻尼系数等静态物理参数，编码为物理特征。
    *   **层级融合：** 在 GCN 的每一层中将物理特征与动态状态特征融合，使网络能根据硬件限制自适应补偿动作。
*   **确定性映射 $M_h$：** 使用一个固定的、特定于硬件的线性算子，将通用的运动原语直接映射为物理关节指令，避免了复杂的重定向过程。

#### 3. 实验设计
*   **仿真环境：** 使用 RaiSim 物理引擎。
*   **数据集与 Benchmark：** 
    *   **CrossDex/YCB Benchmark：** 包含 45 个 YCB 物体。
    *   **GraspXL Benchmark：** 包含来自 ShapeNet 和 PartNet 的大量物体。
*   **对比方法：** 主要对比了 **CrossDex**（目前最先进的跨具身灵巧抓取方法）和 **GraspXL**（单手抓取专家模型）。
*   **具身设置：** 在 Allegro、Shadow、Ability、Schunk 四种手上进行联合训练，在 **LEAP** 和 **Inspire** 两款未见过的手上进行零样本测试。
*   **真实世界验证：** 在 Kinova 机械臂（搭载 LEAP/Inspire 手）和 Piper 机械臂（搭载 Revo2 手）上对 10 种未知物体进行测试。

#### 4. 资源与算力
*   **硬件：** 使用了 **NVIDIA RTX 3090 GPU** 进行训练和推理。
*   **训练时长：** 策略在仿真中训练了 **6000 轮（rounds）**。
*   **并行度：** 每个（手，物体）对实例化了 3 个并行仿真环境。

#### 5. 实验数量与充分性
论文开展了多维度的实验，具有较高的充分性和客观性：
*   **跨具身迁移实验：** 验证了多手联合训练后的零样本泛化能力。
*   **单手迁移实验：** 探讨了仅在一种手上训练时的迁移效果。
*   **消融实验：** 分别验证了运动原语、物理属性注入、层级融合机制以及激活掩码（Mactivation）的必要性。
*   **真实世界部署：** 在三种不同的硬件组合上进行了 150 次以上的真实抓取试验。
*   **扩展性测试：** 在补充材料中甚至测试了向非人形态手（Barrett Hand，3 指 8 自由度）的迁移，证明了框架的普适性。

#### 6. 论文的主要结论与发现
*   **性能卓越：** 在仿真中，该方法对未知手的零样本抓取成功率达到 **85%**，比 CrossDex 提升了 **59.5%**。
*   **物理意识的重要性：** 消融实验证明，注入 URDF 物理先验能显著提升策略在不同连杆长度和驱动限制下的适应性。
*   **动作空间对齐：** 相比直接输出关节角度，基于生物力学原语的动作空间是实现跨具身迁移的关键。
*   **实测有效：** 在真实世界中实现了 **82%** 的平均成功率，证明了从仿真到现实（Sim-to-Real）的良好转化。

#### 7. 优点
*   **端到端一致性：** 避免了传统方法中“中间表示+重定向”带来的运动学冲突问题。
*   **形态通用性：** 提出的形态对齐图表示具有很强的扩展性，不仅限于类人手，也可用于其他多指末端执行器。
*   **物理鲁棒性：** 通过层级注入物理参数，使模型具备了“感知自己身体限制”的能力。

#### 8. 不足与局限
*   **触觉反馈缺失：** 真实世界中的失败案例多集中在小物体上，主要是因为缺乏触觉反馈，导致无法检测微小的接触状态。
*   **硬件尺寸限制：** 在极小尺寸的灵巧手（如 Revo2）上表现略逊，这受限于硬件本身的物理包络能力，而非算法本身。
*   **感知依赖：** 依赖于高质量的物体点云分割，在遮挡严重的复杂场景下性能可能会受限。

（完）
