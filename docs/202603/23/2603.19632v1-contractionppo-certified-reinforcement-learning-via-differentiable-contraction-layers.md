---
title: "ContractionPPO: Certified Reinforcement Learning via Differentiable Contraction Layers"
title_zh: ContractionPPO：通过可微收缩层实现的经认证强化学习
authors: "Vrushabh Zinage, Narek Harutyunyan, Eric Verheyden, Fred Y. Hadaegh, Soon-Jo Chung"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19632v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 足式机器人认证鲁棒规划与控制
tldr: 本研究针对足式机器人在非结构化环境中的鲁棒性问题，提出了ContractionPPO框架。该框架通过在PPO强化学习中引入可微收缩度量层，将性能优化与稳定性证明相结合。利用Lipschitz神经网络参数化收缩度量，确保闭环系统的增量指数稳定性。实验证明，该方法在四足机器人上实现了具备形式化稳定性保证的高性能控制，能有效应对外部扰动并具有良好的实机泛化能力。
selection_source: fresh_fetch
motivation: 强化学习在处理复杂足式机器人运动时虽表现优异，但往往缺乏确保系统在扰动下稳定性的形式化理论保证。
method: 在PPO算法中集成一个由Lipschitz神经网络构成的收缩度量层，通过联合训练学习出能证明系统增量指数稳定性的状态相关度量。
result: 四足机器人硬件实验表明，ContractionPPO在强外部扰动下仍能保持稳健且可证明稳定的运动性能。
conclusion: 将收缩理论与强化学习结合，为开发兼具高性能与严谨稳定性保证的机器人控制策略开辟了新路径。
---

## 摘要
非结构化环境中的足式运动不仅需要高性能的控制策略，还需要形式化保证以确保在扰动下的鲁棒性。控制方法通常需要精心设计的参考轨迹，而这在诸如四足机器人等高维、多接触系统中极具挑战性。相比之下，强化学习（RL）直接学习隐式生成运动的策略，并独特地受益于在训练期间获取特权信息（如完整状态和动力学），而这些信息在部署时是无法获取的。我们提出了 ContractionPPO，这是一个通过在近端策略优化（PPO）强化学习中增加状态相关的收缩度量层，来实现足式机器人经认证的鲁棒规划与控制的框架。该方法使策略在最大化性能的同时，产生一个收缩度量，用以证明模拟闭环系统的增量指数稳定性。该度量被参数化为 Lipschitz 神经网络，并与策略联合训练，可以采用并行方式或作为 PPO 骨干网络的辅助头。虽然收缩度量在实际执行过程中并不部署，但我们推导了最坏情况收缩率的上界，并证明这些上界确保了所学习的收缩度量能够从模拟泛化到现实世界的部署。我们在四足运动上的硬件实验表明，即使在强烈的外部扰动下，ContractionPPO 也能实现鲁棒且经认证稳定的控制。

## Abstract
Legged locomotion in unstructured environments demands not only high-performance control policies but also formal guarantees to ensure robustness under perturbations. Control methods often require carefully designed reference trajectories, which are challenging to construct in high-dimensional, contact-rich systems such as quadruped robots. In contrast, Reinforcement Learning (RL) directly learns policies that implicitly generate motion, and uniquely benefits from access to privileged information, such as full state and dynamics during training, that is not available at deployment. We present ContractionPPO, a framework for certified robust planning and control of legged robots by augmenting Proximal Policy Optimization (PPO) RL with a state-dependent contraction metric layer. This approach enables the policy to maximize performance while simultaneously producing a contraction metric that certifies incremental exponential stability of the simulated closed-loop system. The metric is parameterized as a Lipschitz neural network and trained jointly with the policy, either in parallel or as an auxiliary head of the PPO backbone. While the contraction metric is not deployed during real-world execution, we derive upper bounds on the worst-case contraction rate and show that these bounds ensure the learned contraction metric generalizes from simulation to real-world deployment. Our hardware experiments on quadruped locomotion demonstrate that ContractionPPO enables robust, certifiably stable control even under strong external perturbations.

---

## 论文详细总结（自动生成）

以下是对论文《ContractionPPO: Certified Reinforcement Learning via Differentiable Contraction Layers》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在利用强化学习（RL）获取高性能足式机器人控制策略的同时，提供控制理论中的**形式化稳定性保证**？
*   **背景**：
    *   传统的足式机器人控制依赖于精确的参考轨迹设计，在复杂接触环境下难以建模。
    *   深度强化学习（如PPO）虽然能处理高维非线性动力学，但其策略通常被视为“黑盒”，缺乏在面对外部扰动、传感器噪声或模型失配时的稳定性证明。
    *   **收缩理论（Contraction Theory）**能提供非线性系统的增量指数稳定性保证，但传统上需要人工设计度量矩阵，难以扩展到复杂的机器人系统。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将PPO算法与一个可微的收缩度量层（Contraction Layer）相结合。在训练阶段利用仿真环境中的“特权信息”（如完整状态和动力学），学习一个状态相关的收缩度量矩阵 $M_\phi(x)$，从而在优化奖励函数的同时，强制满足稳定性约束。
*   **关键技术细节**：
    *   **参数化度量**：使用一个Lipschitz连续的神经网络输出下三角矩阵 $\Theta(x)$，构造正定矩阵 $M_\phi(x) = \Theta(x)^\top \Theta(x)$。
    *   **收缩损失函数**：引入基于李雅普诺夫条件的铰链损失（Hinge Loss）：$L_{contr} = \text{ReLU}(\dot{V}_\phi + \alpha V_\phi + \epsilon_\alpha)$。其中 $V_\phi$ 是测地距离的近似，$\alpha$ 是目标收缩率。
    *   **联合优化**：总损失函数 $L_{total}$ 包含了PPO的代理损失、收缩约束损失以及确保度量矩阵条件良好的正则项。
    *   **Lipschitz约束**：通过对策略网络和度量网络进行谱归一化（Spectral Normalization），确保收缩率 $\alpha$ 存在显式的理论下界，从而保证从仿真到现实（Sim-to-Real）的泛化性。
*   **算法流程**：在仿真中并行收集数据 -> 计算变分动力学矩阵 $A_{cl}$ -> 评估收缩条件 -> 反向传播更新策略参数 $\theta$ 和度量参数 $\phi$ -> 部署时仅保留策略网络。

### 3. 实验设计
*   **实验场景**：
    1.  **四足机器人倒立平衡（Handstand）**：在移动平台上保持倒立姿态并限制在半径0.8m的圆圈内。
    2.  **抗风扰实验**：在4.8m/s至9.6m/s的不同风速下测试稳定性。
    3.  **人形机器人（Booster T1）稳定**：验证算法在不同形态机器人上的通用性。
*   **Benchmark（对比方法）**：
    *   **PPO**：标准近端策略优化。
    *   **RMA (Rapid Motor Adaptation)**：基于在线适应的鲁棒RL基准。
    *   **TumblerNet**：一种针对足式机器人稳定性的最新学习方法。
    *   **消融实验**：对比固定收缩度量（$M=I$）与状态相关度量的效果。

### 4. 资源与算力
*   **硬件环境**：台式机，配置为 Intel Core i7-13700F CPU，32GB RAM，**NVIDIA RTX 3080 GPU**。
*   **软件框架**：IsaacLab 仿真环境，利用 RSL-RL 库。
*   **训练规模**：并行运行 **4096个智能体**。
*   **训练时长**：完成完整训练大约需要 **2小时** 墙上时间（Wall-clock time）。

### 5. 实验数量与充分性
*   **实验规模**：进行了数千次仿真回合（Combined 2500 episodes）以及真实的硬件部署实验。
*   **充分性评价**：
    *   **对比全面**：涵盖了从基础PPO到最先进的适应性RL算法。
    *   **跨模态验证**：不仅在四足机器人（Go1）上验证，还扩展到了人形机器人。
    *   **极端环境测试**：引入了训练中未见过的强风扰动（Out-of-distribution），证明了算法的零样本（Zero-shot）鲁棒性。
    *   **客观性**：通过定量指标（如失败率、RMS跟踪误差）和定性视频演示，多维度评估了稳定性。

### 6. 主要结论与发现
*   **稳定性认证**：ContractionPPO 是首个在保持 PPO 高性能的同时，为高维足式机器人提供闭环增量指数稳定性证明的框架。
*   **鲁棒性优势**：在移动平台实验中，ContractionPPO 的失败率为 0%，而 PPO 和 TumblerNet 的失败率接近 100%，RMA 约为 20%。
*   **泛化能力**：即使未针对强风进行训练，该算法也能通过收缩保证确保机器人轨迹保持在有界区域内，并能从剧烈扰动中恢复。
*   **理论验证**：推导出的收缩率上界确保了稳定性度量在存在 Sim-to-Real 间隙时依然有效。

### 7. 优点
*   **理论与实践结合**：将深奥的收缩理论转化为可微的神经网络层，解决了高维系统难以手动设计度量矩阵的痛点。
*   **部署友好**：虽然训练时使用了复杂的收缩层和特权信息，但部署时仅需轻量级的策略网络，不增加计算负担。
*   **强鲁棒性**：提供了比纯数据驱动方法更可靠的边界保证，尤其在安全敏感的应用场景中具有巨大潜力。

### 8. 不足与局限
*   **局部性限制**：稳定性保证仅在训练期间探索过的紧集（Compact Set）状态空间内有效。如果机器人进入完全未知的状态区域，理论保证可能失效。
*   **计算复杂度**：训练期间需要计算变分动力学矩阵（Jacobians），对于自由度极高的系统，自动微分的开销会显著增加。
*   **超参数敏感性**：收缩率 $\alpha$ 和边缘参数 $\epsilon_\alpha$ 的选择存在权衡（Trade-off），过大的 $\alpha$ 会增加学习难度。
*   **环境依赖**：理论推导假设动力学是控制仿射形式且满足 Lipschitz 条件，对于具有高度不连续接触特性的极端情况，理论一致性仍有待进一步探讨。

（完）
