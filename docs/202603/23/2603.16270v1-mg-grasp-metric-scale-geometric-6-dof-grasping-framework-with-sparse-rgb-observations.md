---
title: "MG-Grasp: Metric-Scale Geometric 6-DoF Grasping Framework with Sparse RGB Observations"
title_zh: MG-Grasp：基于稀疏 RGB 观测的度量尺度几何 6-DoF 抓取框架
authors: "Kangxu Wang, Siang Chen, Chenxing Jiang, Shaojie Shen, Yixiang Dai, Guijin Wang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16270v1"
tags: ["query:hr-rob"]
score: 8.0
evidence: 基于稀疏RGB观测和点云重建的6自由度抓取
tldr: 针对传统6-DoF抓取依赖深度传感器或RGB方法几何表示不准的问题，本文提出MG-Grasp框架。该框架利用双视图3D基础模型，仅通过稀疏RGB图像和相机内外参，即可重建出具有度量尺度且多视图一致的稠密点云，从而生成稳定的抓取位姿。实验证明其在GraspNet-1Billion及真实场景中达到了RGB类方法的领先水平，为无深度传感器的机器人操作提供了新方案。
selection_source: fresh_fetch
motivation: 现有的RGB-only 6-DoF抓取方法由于几何表示不准确，难以实现物理上可靠的机器人操作。
method: 利用双视图3D基础模型结合相机参数，从稀疏RGB图像中重建度量尺度的稠密点云并生成抓取位姿。
result: 在GraspNet-1Billion数据集和真实世界实验中，该方法在RGB类6-DoF抓取任务中取得了当前最优性能。
conclusion: MG-Grasp证明了无需深度传感器也能通过稀疏RGB观测实现高质量、高可靠性的机器人抓取。
---

## 摘要
单视图 RGB-D 抓取检测仍然是 6-DoF 机器人抓取系统中的常用选择，这通常需要深度传感器。虽然最近对仅使用 RGB 的 6-DoF 抓取方法进行了研究，但它们不准确的几何表示并不直接适用于物理上可靠的机器人操作，从而阻碍了可靠抓取的生成。为了解决这些局限性，我们提出了 MG-Grasp，这是一种新型的无深度 6-DoF 抓取框架，可实现高质量的物体抓取。利用结合相机内参/外参的双视图 3D 基础模型，我们的方法从稀疏 RGB 图像中重建出度量尺度且多视图一致的稠密点云，并生成稳定的 6-DoF 抓取。在 GraspNet-1Billion 数据集和真实世界中的实验表明，MG-Grasp 在基于 RGB 的 6-DoF 抓取方法中达到了最先进的（SOTA）抓取性能。

## Abstract
Single-view RGB-D grasp detection remains a com- mon choice in 6-DoF robotic grasping systems, which typically requires a depth sensor. While RGB-only 6-DoF grasp methods has been studied recently, their inaccurate geometric repre- sentation is not directly suitable for physically reliable robotic manipulation, thereby hindering reliable grasp generation. To address these limitations, we propose MG-Grasp, a novel depth- free 6-DoF grasping framework that achieves high-quality object grasping. Leveraging two-view 3D foundation model with camera intrinsic/extrinsic, our method reconstructs metric- scale and multi-view consistent dense point clouds from sparse RGB images and generates stable 6-DoF grasp. Experiments on GraspNet-1Billion dataset and real world demonstrate that MG-Grasp achieves state-of-the-art (SOTA) grasp performance among RGB-based 6-DoF grasping methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《MG-Grasp: Metric-Scale Geometric 6-DoF Grasping Framework with Sparse RGB Observations》的结构化技术总结：

### 1. 核心问题与研究背景
*   **核心问题**：传统的 6-DoF 机器人抓取高度依赖 RGB-D 传感器（深度相机），但深度传感器成本高、在室外或面对透明/高反光物体时表现不佳。现有的“仅 RGB”（RGB-only）方法虽然试图摆脱深度硬件，但往往存在几何表示不准、缺乏绝对度量尺度（Metric-scale）以及多视图不一致的问题，导致抓取成功率低。
*   **研究背景**：随着 3D 基础模型（如 DUSt3R, MASt3R）的发展，从图像恢复几何的能力大幅提升。本文旨在利用这些模型，结合已知的相机位姿，构建一个能够生成物理可靠、具备度量尺度的 6-DoF 抓取框架。

### 2. 方法论
MG-Grasp 提出了一套从稀疏 RGB 图像到 6-DoF 抓取位姿的统一流水线，核心步骤包括：
*   **度量尺度深度生成器**：
    *   利用 **MASt3R** 模型提取两两视图间的点云图（Pointmaps）和置信度。
    *   **基于三角测量的尺度恢复**：由于基础模型输出的深度是“比例不确定”的，该模块利用已知的相机外参，通过三角测量计算匹配点的真实物理深度，并以此为基准将所有视图的深度图对齐到真实的度量尺度。
*   **多视图引导抓取生成**：
    *   **两阶段一致性优化**：为了消除视图间的重叠误差，设计了基于 Huber 损失的优化方案。第一阶段优化 3D 空间一致性（点对点距离），第二阶段优化 2D 重投影一致性。
    *   **抓取感知引导**：利用 MobileSAM 进行背景去除，并结合置信度掩码过滤掉不可靠的重建点。
*   **抓取解码**：将精炼后的点云输入 **Local Grasp (LoG)** 模块，通过区域级的特征聚合预测 6-DoF 抓取位姿（位置、旋转、夹爪宽度）。

### 3. 实验设计
*   **数据集与 Benchmark**：主要在 **GraspNet-1Billion** 大规模抓取数据集上进行评估，涵盖了 RealSense 和 Kinect 两种相机的测试集（包含 Seen, Similar, Novel 三类物体）。
*   **对比方法**：
    *   **RGB-D 方法**（作为上限）：GraspNet, TransGrasp, HGGD, FlexLoG。
    *   **RGB-only 方法**：GraspNeRF, VG-Grasp。
    *   **几何基准对比**：将本文方法与基于 VGGT、MASt3R-SLAM、MVSAnywhere 等不同几何后端生成的抓取效果进行了对比。
*   **真实世界实验**：使用 UR5e 机械臂和 Robotiq 夹爪，在包含 25 种日常用品的杂乱场景中进行实机测试。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 4090 GPU** 和 Intel Core i9-13900K CPU 的台式机上运行。
*   **推理耗时**：端到端推理延迟约为 **2.86秒**（5个视图输入）或 **2.13秒**（4个视图输入）。
*   **算力分布**：几何处理阶段（匹配与精炼）占据了总耗时的 77.3%，抓取生成仅占约 10.6%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 GraspNet 数据集上进行了全量评估，对比了 7 种主流方法。
    *   **消融实验**：分别验证了“尺度恢复”和“多视图精炼”模块的有效性。
    *   **视图数量分析**：测试了输入视图从 2 个到 9 个对抓取精度（AP）的影响。
    *   **实机测试**：进行了 40 次抓取尝试，涵盖了透明物体等挑战性场景。
*   **充分性评价**：实验设计较为充分，不仅对比了同类 RGB 方法，还通过消融实验证明了每个模块的增益。实机测试验证了该方法在真实物理环境中的闭环能力，结果客观。

### 6. 主要结论与发现
*   **性能领先**：MG-Grasp 在 GraspNet 数据集上的表现显著优于现有的 RGB-only 方法（如比 VG-Grasp 平均 AP 提升了约 12-14 点），并接近了部分 RGB-D 方法的水平。
*   **视图鲁棒性**：在 4-6 个稀疏视图输入时即可达到性能饱和，证明了其对稀疏观测的高效利用。
*   **透明物体潜力**：在真实实验中，该方法对深度相机难以处理的透明物体表现出较好的抓取能力（成功率 55%），证明了 RGB 几何重建的优势。

### 7. 优点与亮点
*   **度量尺度对齐**：通过三角测量巧妙解决了 3D 基础模型缺乏绝对尺度的问题，使其能直接用于机器人操作。
*   **高精度几何**：两阶段优化显著减少了点云的分层和错位现象，提供了比 MVS 或纯前馈模型更一致的几何表示。
*   **无需深度图训练**：该框架不依赖深度传感器，降低了系统部署成本和硬件限制。

### 8. 不足与局限
*   **实时性不足**：2.86 秒的推理时间对于高速动态抓取任务来说仍然较慢，无法实现高频闭环控制。
*   **特定材质挑战**：对于**极高反光**（镜面）或**近球形**物体，由于难以提取可靠的稠密对应关系，重建几何仍可能出现偏差。
*   **依赖位姿先验**：系统运行前提是已知精确的相机内外参，若相机标定存在误差，会直接影响尺度恢复和精炼效果。

（完）
