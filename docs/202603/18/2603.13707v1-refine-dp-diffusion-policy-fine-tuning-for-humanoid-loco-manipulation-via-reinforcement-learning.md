---
title: "REFINE-DP: Diffusion Policy Fine-tuning for Humanoid Loco-manipulation via Reinforcement Learning"
title_zh: REFINE-DP：通过强化学习进行类人机器人移动-操作协同的扩散策略微调
authors: "Zhaoyuan Gu, Yipu Chen, Zimeng Chai, Alfred Cueva, Thong Nguyen, Yifan Wu, Huishu Xue, Minji Kim, Isaac Legene, Fukang Liu, Matthew Kim, Ayan Barula, Yongxin Chen, Ye Zhao"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13707v1"
tags: ["query:hr-rob"]
score: 10.0
evidence: 通过扩散策略微调实现人形机器人移动操作的分层框架
tldr: 针对人形机器人移动操作中高层规划与底层控制解耦导致的分布偏移和任务失败问题，本文提出 REFINE-DP 框架。该框架通过强化学习（PPO）同时微调预训练的扩散策略（DP）高层规划器和底层控制器，使两者协同优化。实验证明该方法在门禁穿越和长程物体搬运任务中表现优异，显著提升了任务成功率和泛化能力，并能有效处理分布外场景。
selection_source: fresh_fetch
motivation: 旨在解决人形机器人中扩散策略规划器与底层控制器不匹配导致的指令跟踪差、分布偏移及任务失败等挑战。
method: 提出一种分层框架，利用基于 PPO 的扩散策略梯度同时微调高层 DP 规划器和底层 RL 控制器，实现两者的联合优化。
result: "在仿真中实现了超过 90% 的任务成功率，并成功在真实世界动态环境中完成了平滑的自主任务执行。"
conclusion: 研究证明强化学习微调是提升人形机器人移动操作可靠性、克服离线预训练数据局限性的关键手段。
---

## 摘要
类人机器人的移动-操作协同（loco-manipulation）需要在复杂的机器人-环境动力学和长时程任务下，将协调的高层运动规划与稳定的底层全身执行相结合。虽然扩散策略（DPs）在模仿学习方面展现出潜力，但在类人机器人上部署它们面临着严峻挑战：离线训练的运动规划器与底层控制器是解耦的，这会导致指令跟踪效果差、分布偏移累积以及任务失败。对于高维类人系统而言，通过扩展演示数据规模的常规方法成本极高。为了应对这一挑战，我们提出了 REFINE-DP（扩散策略的强化学习微调），这是一个联合优化 DP 高层规划器和基于 RL 的底层移动-操作控制器的分层框架。DP 通过基于 PPO 的扩散策略梯度进行微调以提高任务成功率，同时控制器同步更新以精确跟踪规划器不断演化的指令分布，从而减少降低运动质量的分布不匹配问题。我们在执行移动-操作任务（包括穿过门和长时程物体搬运）的类人机器人上验证了 REFINE-DP。REFINE-DP 在仿真中实现了超过 90% 的成功率，即使是在预训练数据中未见的分布外情况下也是如此，并能在现实世界的动态环境中实现平滑的自主任务执行。我们提出的方法显著优于预训练的 DP 基准，并证明了 RL 微调是实现可靠类人机器人移动-操作协同的关键。https://refine-dp.github.io/REFINE-DP/

## Abstract
Humanoid loco-manipulation requires coordinated high-level motion plans with stable, low-level whole-body execution under complex robot-environment dynamics and long-horizon tasks. While diffusion policies (DPs) show promise for learning from demonstrations, deploying them on humanoids poses critical challenges: the motion planner trained offline is decoupled from the low-level controller, leading to poor command tracking, compounding distribution shift, and task failures. The common approach of scaling demonstration data is prohibitively expensive for high-dimensional humanoid systems. To address this challenge, we present REFINE-DP (REinforcement learning FINE-tuning of Diffusion Policy), a hierarchical framework that jointly optimizes a DP high-level planner and an RL-based low-level loco-manipulation controller. The DP is fine-tuned via a PPO-based diffusion policy gradient to improve task success rate, while the controller is simultaneously updated to accurately track the planner's evolving command distribution, reducing the distributional mismatch that degrades motion quality. We validate REFINE-DP on a humanoid robot performing loco-manipulation tasks, including door traversal and long-horizon object transport. REFINE-DP achieves an over $90\%$ success rate in simulation, even in out-of-distribution cases not seen in the pre-trained data, and enables smooth autonomous task execution in real-world dynamic environments. Our proposed method substantially outperforms pre-trained DP baselines and demonstrates that RL fine-tuning is key to reliable humanoid loco-manipulation. https://refine-dp.github.io/REFINE-DP/

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：REFINE-DP

#### 1. 核心问题与研究动机
*   **核心问题**：人形机器人的“移动-操作协同”（Loco-manipulation）极具挑战，要求高层规划与底层全身控制在复杂动力学下高度同步。
*   **研究背景**：
    *   **扩散策略（DP）的局限**：虽然 DP 擅长捕捉多峰分布的专家行为，但离线训练的规划器与底层控制器往往是解耦的，导致指令跟踪差、误差累积和分布偏移（Distribution Shift）。
    *   **数据成本**：人形机器人自由度高，通过大规模采集专家演示数据来覆盖所有场景的成本极其昂贵。
*   **研究动机**：探索如何通过强化学习（RL）微调，在少量数据的基础上提升 DP 的成功率，并解决规划与控制之间的不匹配问题。

#### 2. 方法论：REFINE-DP 框架
该方法采用**分层架构**，分为三个阶段：
*   **阶段一：数据采集**：利用 VR 遥操作或启发式规划器，驱动预训练的底层 RL 控制器生成专家演示。DP 的动作空间被简化为低维、物理可解释的指令（基座速度、手部 6D 位姿）。
*   **阶段二：扩散策略预训练**：使用采集的轨迹离线训练一个基于 Transformer（DiT）或 MLP 的扩散策略规划器。
*   **阶段三：联合优化（核心创新）**：
    *   **DP 微调**：采用 **DPPO（Diffusion Policy Policy Optimization）** 算法。将扩散过程的去噪步骤嵌入到马尔可夫决策过程（MDP）中，利用 PPO 算法通过环境反馈的稀疏奖励直接更新 DP 参数。
    *   **联合优化**：不仅微调高层 DP，还同步更新底层 RL 控制器。这使得控制器能够适应 DP 演化中的指令分布，减少分布不匹配，提升跟踪精度和运动平滑度。

#### 3. 实验设计
*   **实验场景**：
    1.  **物体抓取**：走到桌边抓取盒子。
    2.  **长程搬运**：抓取盒子并跨越空间放置到另一张桌子（持续约 40 秒）。
    3.  **开门与穿越**：人形机器人自主识别门把手、开门并走过去。
    4.  **阶梯辅助检索**：踏上平台获取高处物体。
*   **基准方法（Benchmarks）**：
    *   **DiT/MLP Planner**：仅预训练，不进行微调。
    *   **MLP-FT**：对 MLP 架构进行 RL 微调。
    *   **Residual RL**：在冻结的 DP 之上学习残差补偿策略。
    *   **DiT-FT (Ours)**：仅微调 DP。
    *   **Joint Optimized (Ours)**：联合优化 DP 和底层控制器。

#### 4. 资源与算力
*   **硬件平台**：Booster T1 人形机器人（29 自由度）。
*   **计算设备**：
    *   **策略电脑**：AMD 7945HX CPU，**NVIDIA RTX 4060 GPU**。
    *   **感知**：使用 MoCap（动捕系统）获取机器人和物体的位姿（90Hz）。
*   **运行频率**：DP 规划器运行在 10Hz，底层 RL 控制器运行在 50Hz。
*   **训练环境**：使用 NVIDIA IsaacLab 进行大规模并行仿真训练。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在仿真中针对 4 类任务进行了大量测试，每组对比实验通常包含 100 次随机试验。
    *   **消融实验**：对比了不同骨干网络（Transformer vs MLP）、有无微调、有无联合优化、以及数据规模对成功率的影响。
    *   **分布外（OOD）测试**：通过课程学习（Curriculum Learning）测试了机器人在未见过的初始位置下的泛化能力。
*   **充分性评价**：实验设计较为全面，涵盖了从算法性能、数据效率到运动质量（跟踪误差、平滑度）的多个维度，且包含真机验证，具有较高的说服力。

#### 6. 主要结论与发现
*   **成功率大幅提升**：REFINE-DP 在仿真中将任务成功率从预训练的 50-70% 提升至 **90% 以上**。
*   **数据效率极高**：仅需 **50 条** 演示轨迹配合微调，即可达到纯预训练模式下使用 **1000 条** 轨迹的效果。
*   **运动质量优化**：联合优化显著降低了手部跟踪误差（位置误差降低，姿态误差降低达 50%），并减少了运动抖动。
*   **鲁棒性增强**：微调后的策略在面对环境扰动（如物体位置改变）时表现出更强的重试和自我修复能力。

#### 7. 优点与亮点
*   **联合优化思想**：打破了“规划器只管发指令，控制器只管跟指令”的传统解耦模式，通过协同进化解决了分布偏移问题。
*   **分层动作空间简化**：将 DP 的输出设定为基座速度和手部姿态，而非全身关节角，降低了学习难度且更易于 Sim-to-Real 迁移。
*   **高效微调**：证明了在人形机器人领域，RL 微调比单纯堆砌离线数据更具成本效益。

#### 8. 不足与局限
*   **感知依赖**：实验高度依赖 MoCap 系统获取精确的物体位姿，尚未完全实现基于视觉（如相机像素）的端到端闭环。
*   **真机性能下降**：虽然仿真中成功率超 90%，但真机实验受限于传感器噪声和机械抖动，成功率降至 50-70% 左右。
*   **任务复杂度**：虽然涵盖了开门和搬运，但尚未涉及更复杂的双臂精细协作或极端非结构化地形。

（完）
