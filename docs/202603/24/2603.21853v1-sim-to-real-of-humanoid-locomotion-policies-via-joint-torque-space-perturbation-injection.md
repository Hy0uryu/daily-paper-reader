---
title: Sim-to-Real of Humanoid Locomotion Policies via Joint Torque Space Perturbation Injection
title_zh: 通过关节力矩空间扰动注入实现类人机器人运动策略的仿真到现实迁移
authors: "Junhyeok Rui Cha, Woohyun Cha, Jaeyong Shin, Donghyeon Kim, Jaeheung Park"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21853v1"
tags: ["query:hr-rob"]
score: 9.0
evidence: 通过关节力矩空间扰动实现人形机器人运动策略的从仿真到现实迁移
tldr: 本研究针对类人机器人运动控制中的仿真到现实（Sim-to-Real）迁移问题，提出了一种在关节扭矩空间注入状态相关扰动的新方法。不同于依赖固定参数集的传统领域随机化，该方法利用神经网络作为灵活的扰动生成器，能够模拟非线性执行器动力学和接触顺应性等复杂不确定性。实验证明，该方法显著提升了策略在面对未知现实差距时的鲁棒性，成功实现了类人机器人的稳定实机部署。
selection_source: fresh_fetch
motivation: 传统的领域随机化方法受限于固定参数集，难以捕捉如非线性执行器动力学等复杂的、随状态变化的现实差距。
method: 在前向仿真过程中，利用神经网络生成并向关节扭矩输入注入与状态相关的扰动，以模拟更广泛的物理不确定性。
result: 该方法使类人机器人运动策略在仿真和真实世界部署中，对复杂且未见的现实差距展现出优越的鲁棒性。
conclusion: 通过在扭矩空间注入状态相关的扰动，可以比传统参数随机化更有效地提升类人机器人控制策略的泛化与迁移能力。
---

## 摘要
本文提出了一种利用仿真经验训练控制策略的现有仿真到现实（sim-to-real）方法的新型替代方案。与以往通常依赖于固定有限参数集上的领域随机化（domain randomization）方法不同，所提方法在正向仿真过程中向输入关节力矩注入了与状态相关的扰动。这些扰动旨在模拟比标准参数随机化更广泛的现实差距（reality gaps），且无需额外的训练。通过使用神经网络作为灵活的扰动生成器，该方法可以表示复杂的、与状态相关的各种不确定性，例如参数随机化无法捕捉的非线性执行器动力学和接触顺性。实验结果表明，所提方法使类人机器人运动策略在仿真和现实世界部署中，都能针对复杂的、未见的现实差距表现出卓越的鲁棒性。

## Abstract
This paper proposes a novel alternative to existing sim-to-real methods for training control policies with simulated experiences. Unlike prior methods that typically rely on domain randomization over a fixed finite set of parameters, the proposed approach injects state-dependent perturbations into the input joint torque during forward simulation. These perturbations are designed to simulate a broader spectrum of reality gaps than standard parameter randomization without requiring additional training. By using neural networks as flexible perturbation generators, the proposed method can represent complex, state-dependent uncertainties, such as nonlinear actuator dynamics and contact compliance, that parametric randomization cannot capture. Experimental results demonstrate that the proposed approach enables humanoid locomotion policies to achieve superior robustness against complex, unseen reality gaps in both simulation and real-world deployment.

---

## 论文详细总结（自动生成）

以下是对论文《Sim-to-Real of Humanoid Locomotion Policies via Joint Torque Space Perturbation Injection》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何弥合机器人从仿真到现实（Sim-to-Real）的“现实差距”（Reality Gap）。
*   **研究背景**：深度强化学习（DRL）在机器人控制中表现出色，但直接在真机训练存在安全和效率问题。传统的**领域随机化（Domain Randomization, DR）**是主流方案，但其局限性在于：
    1.  **参数有限**：仅能随机化仿真器暴露的有限参数（如质量、摩擦力）。
    2.  **表达力不足**：无法模拟复杂的非线性动力学（如执行器的迟滞、齿隙、接触顺性等）。
*   **研究动机**：作者认为现实差距最终体现为关节力矩层面的偏差。因此，提出通过在关节力矩空间注入“状态相关”的扰动，来模拟比传统参数随机化更广泛、更复杂的物理不确定性。

### 2. 方法论
*   **核心思想**：将领域随机化视为一种非线性的关节力矩扰动函数。既然仿真器自带的参数随机化表达力有限，不如直接用一个**随机初始化的神经网络**来生成扰动。
*   **关键技术细节**：
    *   **扰动注入**：在正向仿真中，最终输出力矩 $\tau = \tau_{input} + \tau_{\phi}(s)$。其中 $\tau_{\phi}$ 是一个小型多层感知机（MLP）。
    *   **权重随机化**：在每个训练回合（Episode）开始时，随机采样 MLP 的权重 $\phi$（使用 Xavier 初始化），且在回合内保持固定。这使得策略必须学会应对各种不同特性的“虚拟物理环境”。
    *   **状态相关性**：扰动函数 $\tau_{\phi}$ 的输入是机器人的特权观测信息（Privileged Observation），使其能够根据机器人当前姿态、速度等状态产生复杂的非线性扰动。
    *   **训练框架**：结合了**去噪世界模型学习（DWL）**。使用 GRU 编码器处理历史观测，并通过重建特权信息来学习鲁棒的潜在表示。
    *   **步态引导**：采用基于 Raibert 启发式的在线轨迹生成，为奖励函数提供参考，从而在无需运动捕捉数据的情况下实现自然步态。

### 3. 实验设计
*   **实验平台**：全尺寸类人机器人 **TOCABI**（高 180cm，重 100kg）。
*   **对比基准（Benchmarks）**：
    1.  **标准 DR**：随机化摩擦力、质量、质心、阻尼等参数。
    2.  **ERFI（扩展随机力注入）**：在执行器端注入随机偏差和噪声。
*   **实验场景**：
    *   **Sim-to-Sim**：在 Isaac Gym 训练，在 MuJoCo 中测试未见的动力学（如加入关节弹簧、软地表、粗糙地形）。
    *   **Sim-to-Real**：直接将策略部署到 TOCABI 真机，测试在实验室平滑地面及铺设了 6cm 厚软垫（越野场景）上的表现。
    *   **分布外（OOD）测试**：给机器人增加额外负载（电池、配重块）、更换不同物理特性的足端。

### 4. 资源与算力
*   **仿真器**：使用 NVIDIA **Isaac Gym** 进行大规模并行训练。
*   **训练规模**：同时运行 **4,096** 个并行环境。
*   **样本量**：每次策略更新收集 98,304 个样本，总计进行 10,000 次更新。
*   **算力说明**：文中提到使用了 GPU 加速仿真，但**未明确指出具体的 GPU 型号（如 A100 或 RTX 4090）及具体的训练总时长**。

### 5. 实验数量与充分性
*   **实验数量**：涵盖了消融实验（参考轨迹的影响）、不同控制模式（位置 vs 力矩控制）的鲁棒性验证、多随机种子测试（3个种子）以及多种 OOD 硬件修改测试。
*   **充分性评价**：实验设计较为充分。通过在 MuJoCo（不同于训练环境的物理引擎）中进行测试，有效地证明了算法的泛化能力。真机实验包含了负载变化和地形变化，具有较强的说服力。
*   **客观性**：对比了当前主流的 DR 和 RFI 方法，并使用了 RMSE（均方根误差）和成功率等量化指标，评价较为客观。

### 6. 主要结论与发现
*   **鲁棒性飞跃**：所提方法在面对“未见过的”动力学差距（如关节弹性、地表极度顺性）时，表现远超标准 DR。在某些极端测试中，DR 策略直接崩溃，而本方法仍能维持行走。
*   **泛化能力**：神经网络生成的扰动能够覆盖比参数随机化更广的函数空间，使策略学会了更保守且稳健的平衡策略。
*   **零样本迁移**：成功在 100kg 级的重型类人机器人上实现了零样本（Zero-shot）迁移，且在增加额外负载和面对软垫地形时保持了极高的稳定性。

### 7. 优点
*   **表达力强**：突破了仿真器参数定义的限制，能够模拟现实中难以建模的复杂非线性效应。
*   **通用性高**：该方法不依赖于特定的控制模式（位置或力矩控制均适用），且不需要采集真机数据进行系统辨识。
*   **实现简单**：相比于复杂的执行器建模，随机化一个小网络权重的做法在工程上非常易于实现。

### 8. 不足与局限
*   **启发式倾向**：扰动权重的采样范围（$\sigma_{lim}$）仍需人工调参。如果扰动过大，可能导致策略过于保守（如步幅过小）；如果过小，则起不到鲁棒化作用。
*   **物理可解释性欠缺**：虽然神经网络能模拟各种扰动，但这些扰动并不一定对应真实的物理现象，可能导致策略学习到一些应对“非物理扰动”的奇怪行为。
*   **实验规模限制**：真机实验虽然成功，但主要集中在直线行走和简单转向，尚未展示更复杂的动态动作（如奔跑或跳跃）在扰动注入下的表现。

（完）
